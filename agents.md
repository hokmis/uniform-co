# AI Agent 接手指南

## 正式應用目前狀態（2026-09-21）

正式 Next.js 應用已部署至 [uniform-co.vercel.app](https://uniform-co.vercel.app/)，GitHub `Kevin72333/uniform-co` 的 `main` 分支是目前交付來源，Supabase 負責登入、PostgreSQL、RLS 與 Storage。正式應用的功能資料來源是 `src/`、`src/domain/`、`src/server/` 與 `supabase/migrations/`；[`prototype/uniform-management-prototype.html`](./prototype/uniform-management-prototype.html) 仍是獨立的內容／流程原型，不是正式資料來源。

## 最新 AI 接手快照（2026-09-21）

### 接手第一步

`main` 已包含目前完成的功能、安全防線、migration、workflow、測試、System Guide 與文件。接手後先讀本文件，再讀 `README.md`，接著用 `git status --short`、`git diff` 與 `git log -5 --oneline` 確認現況；目前工作樹保留使用者既有的 `package.json`／`package-lock.json` 修改與未追蹤 `prototype/`，除非需求明確指向這些項目，否則不要納入提交。使用者已要求每次修改驗證完成後自動推送 GitHub；推送前先 fetch／比較 `origin/main`，只 stage 本次檔案，不執行 reset／clean 或整檔覆寫。

Repository-local 最新完整驗證基線（2026-09-21）為：181 個 Vitest files、770/770 tests，另有 6 個 production read-model generator Node tests；`npm run lint`、`npm run typecheck`、`npm run build` 與 `git diff --check` 均已通過。ERP 機構選項沿用帳號範圍快照，同帳號背景刷新不會鎖住批次建立，帳號切換時選項與選取值都以目前帳號及快照為準。GitHub CI 現使用 Node 22，避免 Node 20 缺少 Supabase Realtime 測試所需的原生 WebSocket。CI 直接執行 `node --test scripts/database/generate-production-read-model-sql.test.mjs`；產生器／唯讀 verifier 現涵蓋 migrations `0099`–`0132`、19 個 views、24 個 indexes、13 個 app-facing workflow RPCs 與 28 個 RPC prerequisites，並驗證 `0129` 的 overview `account_id`。正式人資請領流程已完成一筆正式站端到端交易與唯讀驗收，不代表其他 production cutover gate 自動完成。2026-09-21 最新 production 唯讀 verifier 為 relations `47/47`、base functions `28/28`、views `0/19`、indexes `0/24`、callable RPCs `0/13`、overview account binding `false`，冗餘索引 `6`；不得把 repository tests 當成正式資料庫已更新證據。另以唯讀 `EXPLAIN ANALYZE` 單次量測四個總覽 fallback view，DB execution time 約 1.8–4.3 ms、shared disk reads 為 0；此僅代表目前資料量的資料庫端成本，不含 PostgREST RTT、Auth／React hydration 或瀏覽器首屏時間，不可當正式 RUM／P95 證據。

### 正式工作台讀取路徑資料庫待修復（0099–0132）

`ReturnPanel` 的「確認並完成」與補庫送出、五類庫存更正都使用原子 wrapper，讓一項業務動作由多個 PostgREST round trips 收斂為單一資料庫 transaction／往返；底層驗證、冪等、鎖、RLS 與 audit 仍由既有 RPC 負責。正式 project 的唯讀 verifier 顯示 19 個 read views、24 個 indexes、13 個 atomic workflow wrappers 尚未 ready，且 migration ledger 僅至 `0067`。`supabase/manual/restore-application-read-models.sql` 已從 migrations `0099`–`0132` 產生，會先檢查 47 個來源 relation 與 28 個底層函式，再在一個 schema-only transaction 建立／修復 views、indexes、13 wrappers 並檢查 postflight；SQL Editor 不會修補 migration ledger，也不變更業務資料。先在隔離 staging 執行與驗證，再依 `supabase/manual/README.md` 安排正式維護時段；索引 DDL 可能暫時阻擋寫入。不得以 `db push` 代替 schema／ledger 對帳，也不得用正式業務交易測試。修復後需重新執行唯讀 verifier，目標為 views `19/19`、indexes `24/24`、callable RPCs `13/13`、overview binding `true`、冗餘索引 `0`。

### 這一輪已落地的重要能力

1. **Durable import worker／Edge adapter**：線上站可使用 `supabase/functions/import-worker` 按需執行既有 durable import RPC，不需常駐 worker 主機；`DurableImportPanel` 只觸發同源 Supabase Edge Function，不直接呼叫 worker RPC。`scripts/import/import-worker.ts` 與 Edge Function 共用 `job_import_worker` actor、Storage／SHA-256 驗證、chunk lease、idempotency 與 APPLY fencing；Edge 每次請求最多連續處理四個同階段 chunk，並在同一請求內重用解析預覽，前台每次先觸發 Edge 再立即讀回狀態，輪詢間隔降為 2.5 秒，不改變 DB 的 lease／cursor／APPLY 邊界。`npm run import:worker -- --once` 仍供受保護 staging smoke，移除 `--once` 才持續輪詢。前台支援上傳衝突自動續傳（409 conflict 視為已存在並接續驗證）、worker 觸發後立即讀回批次狀態、友善中文狀態標籤與確認後立即執行 APPLY；進入 `VALIDATED` 時直接顯示預覽確認，不讓使用者只看到「正在準備」。Edge secrets／同名 LOGIN 設定見 `docs/deployment/edge-import.md`；此 repository push 不等於 Supabase Function 已部署。
2. **Storage cleanup 與 retention**：`0071`／`0072` 是 Storage bytes eligibility 的 DB 權威；`0073`／`0074` 把 import staging 可清 payload 與永久 evidence 分離。`job_storage_cleanup` 與 `job_import_retention` 是不同最小權限角色，不得合併或互相擴權。兩個 GitHub workflow 的排程都刻意只 dry-run；destructive execute 必須保留雙 gate 與真實 staging smoke。
3. **Backup／restore fail-closed**：generation 有 manifest lock／source stability／完整集合與 SHA-256 驗證；DB metadata 記錄八個關鍵 application table counts，Auth metadata 記錄 users／identities／MFA factors。Restore 先驗空目標、再解密與還原；Storage 固定五個 private bucket allowlist、拒絕 duplicate target／unsafe path／409 conflict，每個 POST 後還要 GET read-back 並重新比 byte length／SHA-256。舊 generation 缺 `application_counts` 時只做 backward-compatible Auth count 驗證。
4. **Deployment safety harness**：`deployment:preflight`、`deployment:smoke`、`deployment:active-smoke`、`deployment:renderer-smoke` 都已提供 fail-closed gate；真實 staging credentials、migration、RLS/Auth/Storage/signed URL/concurrency 才能產生正式 evidence。本機 contract tests 不能替代這些結果。
5. **Error monitoring**：`instrumentation.ts` → `src/server/error-monitoring.ts` 只輸出 allowlisted 結構化欄位與不可逆 fingerprint；`src/app/error.tsx` 只顯示通用錯誤。禁止把 raw message／stack／body／headers／Authorization／cookie／token／query／DB URL 加回 log。外部 provider／alert／incident smoke 尚未取得。
6. **Capacity gate**：`capacity:plan` 只把假設與實測分開計算，沒有 `STAGING_SYNTHETIC` 三年實測、尖峰負載、完整還原與 quota usage 時必須維持 `NOT_VALIDATED`。不要把 example assumptions 或模型估算升格成 production evidence。
7. **Production release evidence**：`deployment:release-gate` 集中 17 個 production cutover gate；`deployment:release-evidence` 對 ignored 的正式 evidence 檔做 init／record／block，使用 lock、重讀與原子替換避免 lost update。版本化 `release-evidence.example.json` 永遠保持 `NOT_READY`，不得修改成「全 PASS」示範正式完成。
8. **SYSTEM_ADMIN System Guide**：`docs/system-guide/` 是使用者、管理者與 AI Agent 三份同源 Markdown；`/system-guide` 使用 `WorkspaceShell initialSystemGuide`，保留原工作區左側導航並在右側顯示文件。文件按鈕來自 `src/domain/system-guide.ts` 的固定 allowlist metadata，不等待 API 才建立；`WorkspaceShell` 會預取 `/system-guide` route bundle，`use-system-guide-access.ts` 會將受保護文件請求與本人可由 RLS 讀取的 SYSTEM_ADMIN 權限檢查並行，並把正向結果快取在目前分頁。`GET /api/system-guide` 每次仍以 bearer session、有效 `app_accounts` 與 `authorizeSystemAdmin()` 重新授權；不得把 sessionStorage 或隱藏入口當成安全邊界，也不得改用 `dangerouslySetInnerHTML`。
9. **共用管理清單**：參考 SPSV29 的大型清單操作後，以 `src/app/ManagementCatalogTable.tsx` 集中帳號、商品、組織、員工、兩倉庫存、營運報表、採購差異原因碼、更正歷史、CEO 待核版本、換季採購決策品項、換季需求登記與待發貨需求的欄位顯示、密度、每頁筆數、資料範圍、排序表頭與首末頁導覽；`src/app/CorrectionHistoryTable.tsx` 以共用表格組合更正歷史的單號／狀態／差額／原因搜尋與狀態篩選；CEO 待核版本的搜尋／排序純規則在 `src/domain/seasonal-approval.ts`，採購品項的搜尋／排序純規則在 `src/domain/seasonal-procurement.ts`，需求登記的搜尋／排序純規則在 `src/domain/seasonal-demand.ts`，待發貨需求的搜尋／排序純規則在 `src/domain/warehouse-shipment.ts`；其他 page／column 規則在 `src/domain/management-catalog.ts`，業務篩選排序分別在各自 domain module。帳號目錄另由 `src/domain/account-directory.ts` 先建立角色索引並展開 `SYSTEM_ADMIN` 有效角色，避免 render 時逐帳號掃描 `user_roles`。資料查詢、RLS、匯出稽核與 mutation 仍留在各業務 module；沒有受保護 bulk RPC 前，不得加入批次刪除／停用。維護規則見 `docs/architecture/management-catalog.md`。
10. **登入與工作區 Session**：SSO 登入熱路徑只做本地 JWT／project ref 驗證與必要資料查詢，不掃描遠端 Auth Admin `listUsers`；中央驗票與 target `auth/v1/user` Session 查詢並行，帳號只由本地 `app_accounts` 的 `id`／`auth_user_id`／`email_snapshot` 解析，角色以同一次 embedded `user_roles(role_code)` 讀回；`0115_sso_account_email_read_index.sql` 為 email 對應建立 partial read index。若舊 schema cache 或測試 double 沒有嵌入欄位，才退回單獨角色查詢。若 target Session 的 user id 與 `app_accounts.auth_user_id` 相同，直接沿用已驗證 Session 並跳過 `getUserById`；只有不匹配或需要建立新 Session 才呼叫單筆 Auth Admin。`WorkspaceShell` 使用單一 `useAuthSession`，`useWorkspaceIdentity` 以一次 `app_accounts` 嵌入 `user_roles` self-read 提供目前帳號／角色，並以 Auth user id 綁定讀取結果，切換帳號時先清空舊身份再放行面板；身份讀取沿用共用的單次 session refresh/retry。`SYSTEM_ADMIN` 的有效角色在前端以共用 role capability helper 展開，資料庫則由 `0088_system_admin_all_roles.sql` 的 `private.has_role` 統一授權。`SeasonalDemandPanel`、`WarehouseShipmentPanel` 與 System Guide 沿用該 context；`RetainedPanelSet` 的未造訪模組 lazy mount 不變。工作區 Session 的 render boundary 與 token hook 規則見 [`docs/architecture/ui-workspaces.md`](./docs/architecture/ui-workspaces.md)，契約測試見 `src/domain/workspace-session-contract.test.ts`。
   - **Session 初始化去重**：`useAuthSession` 只透過 `onAuthStateChange` 的 `INITIAL_SESSION` 與後續事件更新 Session，不另呼叫 `auth.getSession()`；避免重複讀取與初始 Promise 晚回覆覆蓋新 Auth event。待綁定續接仍由同一事件流觸發，見 `src/domain/auth-session-contract.test.ts`。
   - **登入首屏 bundle**：匿名 `/login` 由 `src/app/AuthLanding.tsx` 共用品牌版型與 SSO 綁定提示，不載入整個 `WorkspaceShell`；`AuthPanel` 僅在帳號欄位取得焦點時動態載入 Supabase browser SDK，登入仍由既有 Supabase `signInWithPassword` 與 server-guarded `/app` 完成。`scripts/performance/login-bundle-budget.mjs` 將 `/login` 初始 JS 上限設為 550 KiB；目前本機 build First Load JS 為 107 kB、manifest 為 362,279 bytes／5 chunks，修改前為 653,322 bytes。這是 bundle 尺寸比較，不能當作正式站 FCP 或登入耗時驗收。
11. **人資請領讀取穩定性**：`src/lib/supabase-session.ts` 集中判斷暫時 JWT 不同步並限制單次 refresh/retry；庫存可用量、人資需求主檔、需求查詢明細與待發貨清單都先等待工作區身份，再以同一 Session 批次查詢，其他權限／Schema 錯誤維持安全失敗訊息。`src/lib/supabase-session.test.ts` 覆蓋一次重試與不洩漏 raw error。
   - **人資表單草稿保留**：`src/domain/hr-request-workflow.ts` 的 `preserveHrRequestDraftLines` 與 `preserveHrRequestIncreaseDraft` 讓 `HrRequestWorkbench` 在主檔／庫存重新讀取時只補空白表單，不覆蓋正在編輯的明細；讀取失敗只停用送出並保留輸入，新增表單載入流程時必須沿用這個 seam。
   - **人資表單區域重試**：`HrRequestWorkbench` 提供「重新載入資料」按鈕，重試只遞增本面板的 `dataReloadToken` 並重跑既有主檔／可用量讀取，不觸發整頁刷新；刷新期間保留目前可用快照與已填明細，日期／備註／既有明細仍可編輯，只有送出等待新快照，已送出且尚未按「修改本張需求」仍維持唯讀，只有讀取失敗才將 `dataReady` 設為 false；按鈕不可在讀取或送出期間重複執行，文件與契約測試需保留這個可恢復操作。
   - **Refresh flight 去重**：`src/lib/supabase-session.ts` 以 `WeakMap<SupabaseClient, Promise<boolean>>` 共享並行的 `auth.refreshSession()`；新增資料 panel 仍須呼叫 `retrySupabaseQueriesAfterSessionRefresh`，不得自行刷新 Session 或把 permission／schema error 當成暫時 JWT 問題。
   - **讀取快照保留**：`src/domain/read-refresh.ts` 統一商品、組織、員工與兩倉庫存清單的刷新規則；只有既有快照且所有錯誤都屬短暫 Session 同步錯誤時才保留 rows，權限／schema／資料錯誤仍清空並 fail-closed，避免正常重試造成空畫面也避免 stale rows 掩蓋權限撤銷。契約由 `read-refresh.test.ts` 與 `read-refresh-ui-contract.test.ts` 保護。
   - **報表切換快照**：`ReportingPanel` 以 `reportName`／`loadedReportName` 分離使用者選擇與最後成功資料；切換或短暫同步錯誤時保留上一份完整且欄位一致的報表，新資料成功讀回後才替換，權限／schema 錯誤仍清空。契約由 `reporting-read-path-contract.test.ts` 保護。
   - **需求查詢競態 fencing**：`HrRequestHistoryPanel` 的清單與明細各自以讀取序號拒絕晚到的舊回應；重新整理／工作流事件保留既有快照，快速切換需求時只顯示目前選取需求的明細，短暫 Session 同步錯誤不清空同一需求的資料，身份／權限／schema 錯誤仍 fail-closed。契約由 `hr-request-read-path-contract.test.ts` 保護。
   - **交易身份 readiness**：`PurchaseReceiptPanel`、`ReturnPanel`、`PurchaseReceiptCorrectionPanel` 與 `ReturnCorrectionPanel` 都先等待 `useWorkspaceSession` 的有效 Session／唯一 `accountId`／面板 active，再開始受保護讀取；不要讓 panel 只因 browser client 已建立就提前查詢 Supabase。
   - **交易來源明確選擇**：入庫、退回、盤點、ERP 匯出、補庫、人資請領、換季需求、PDF 單據與五個更正工作台都不再把第一筆清單資料當成使用者選擇；只有既有選擇仍存在時才保留，否則回到「請選擇」。更正恢復只接受有效的既有草稿來源，建立另一筆更正會清除舊來源並要求重新選擇。入庫／退回／盤點會等使用者選定採購單、發貨需求或倉庫後才讀取相依明細／帳面庫存；人資請領新增明細、補庫與換季需求也必須明確選員工／品號／活動。契約由 `correction-history-read-contract.test.ts`、`hr-request-ui-contract.test.ts`、`seasonal-read-path-contract.test.ts`、`reporting-read-path-contract.test.ts` 與 `warehouse-operation-contract.test.ts` 保護。
   - **工作區受保護讀取 readiness**：`OverviewDashboard`、`ReportingPanel`、`EmployeeImportPanel`、`ErpExportPanel`、`PdfArtifactPanel`、`ProcurementReasonCodePanel`、`SeasonalApprovalPanel`、`SeasonalCampaignPanel`、`SeasonalProcurementPanel`、`SeasonalDemandPanel`、`ReplenishmentPanel`、`WarehouseShipmentPanel`、`ProductCatalogPanel`、`OrganizationCatalogPanel`、`EmployeeCatalogPanel` 與 `InventoryAvailabilityPanel` 也必須先通過同一 `useWorkspaceSession` 的 `identityReady`，才可讀取 view／主檔、恢復冪等操作或啟動 artifact 輪詢；身份失敗以衍生狀態顯示，不在 effect 內同步 setState。契約由 `reporting-read-path-contract.test.ts`、`seasonal-read-path-contract.test.ts`、`warehouse-operation-contract.test.ts` 與 `read-refresh-ui-contract.test.ts` 保護。
12. **帳號管理持久化雙重驗證與 SYSTEM_ADMIN Umbrella 權限**：`0088_system_admin_all_roles.sql` 將 `private.has_role` 全域擴充，讓 `SYSTEM_ADMIN` 作為全域能力傘，自動兼具所有六大業務角色的權限；前端 `src/domain/account-roles.ts` 與 session context 同步展開有效角色。`src/server/account-admin.ts` 在使用者個人資料（`updateProfile`）、帳號狀態（`setStatus`）、單一角色賦權（`setRole`）與批次角色替換（`setRoles`）後，強制自 DB 重新讀取並以 `sameAccountProfile` 及 `sameRoleSet` 逐欄比對持久化結果，防止 RPC 假成功；`src/app/AccountAdminPanel.tsx` 直接使用 `useWorkspaceSession` 的既有 Session 呼叫受保護管理 API，並在 Session／工作區身份尚未就緒時不啟動清單與管理資料讀取，窗口範圍查詢只取目前選取帳號的 `account_id`，避免首屏讀取所有帳號的範圍資料；同時套用已查核的 account／role／scope 結果到目前清單，`src/domain/account-admin-local-state.ts` 集中本地同步規則，避免每次成功保存後再次查詢五組清單資料，手動重新整理仍會完整讀取。
13. **讀取路徑與資料庫彙總**：`0098_read_path_indexes.sql` 為常用啟用主檔、需求／發貨／補庫、換季、CEO 審核明細與庫存流水查詢補上可重複套用的索引；`0099_hr_request_history_view.sql` 建立 security-invoker 的需求查詢摘要 view 與必要索引；`0100_reporting_join_indexes.sql` 補上可用量、請領明細、採購入庫與稽核活動 feed 的反向／聚合 join 索引；`0101_transaction_read_indexes.sql` 補上入庫／退回／更正工作台依狀態、日期與來源單據查詢的部分索引；`0102_correction_history_read_indexes.sql` 補上人資、盤點與調撥更正歷史依來源單據的反向索引；`0103_warehouse_shipment_queue_view.sql` 把倉庫待發貨清單的兩種來源與 DRAFT 草稿收斂成 security-invoker view；`0104_hr_request_option_views.sql` 把人資請領的有效員工／組織隸屬與品號／兩倉可用量收斂成兩個 security-invoker option view；`0105_warehouse_transfer_correction_source_view.sql` 再把調撥更正的已完成發貨／補庫來源與明細收斂成單一 security-invoker view；`0106_stocktake_correction_source_view.sql` 將啟用倉庫的已完成盤點與明細收斂成單一 security-invoker view；`0107_receipt_return_correction_source_views.sql` 將已完成入庫／退回主單與明細各自整理成單一 security-invoker view；`0108_hr_issue_correction_source_view.sql` 將已發放需求與發放明細整理成單一 security-invoker view；`0109_seasonal_procurement_queue_view.sql` 將 CEO 核准採購品項與既有採購決策整理成單一 security-invoker view。`HrRequestWorkbench` 使用 `v_hr_request_employee_options`／`v_hr_request_item_options`，`HrRequestHistoryPanel` 使用 `v_hr_request_history`，`WarehouseShipmentPanel` 使用 `v_warehouse_shipment_queue`，`WarehouseTransferCorrectionPanel` 使用 `v_warehouse_transfer_correction_sources`，`StocktakeCorrectionPanel` 使用 `v_stocktake_correction_sources`，`PurchaseReceiptCorrectionPanel`／`ReturnCorrectionPanel`／`HrIssueCorrectionPanel` 使用對應更正來源 view，`SeasonalProcurementPanel` 使用 `v_seasonal_procurement_queue`，入庫工作台先選採購單再載入明細，需求明細仍採選取後載入；總覽的最近活動與工作佇列依明確時間／識別欄排序。`0115_sso_account_email_read_index.sql` 補上本地 SSO email 對應索引；`0116_correction_history_view.sql` 再將五個更正模組的六種來源明細收斂成單一 security-invoker view，前端 `loadCorrectionHistory` 優先一次讀取；尚未套用時先依來源專屬複合外鍵嵌入 correction note，將串行主單／明細讀取合併成一次，只有 PostgREST 關聯快取錯誤才退回舊兩段讀取；`0117_replenishment_shipment_lines_view.sql` 將補庫明細與 GENERAL 可用量收斂成單一 security-invoker view，前端由 `src/lib/replenishment-shipment-read.ts` 優先單次讀取，未套用時才 fallback；`0118_overview_core_view.sql` 將總覽四個核心摘要收斂成單一 security-invoker view，前端由 `src/lib/overview-dashboard-read.ts` 優先單次讀取，未套用時才 fallback 到原四組 view；`0119_warehouse_shipment_lines_view.sql` 將 HR 發貨明細與需求品項快照收斂成單一 security-invoker view，前端由 `src/lib/warehouse-shipment-read.ts` 優先單次讀取；未套用時先以複合外鍵嵌入 `hr_request_items` 一次查讀，僅 PostgREST 關聯快取錯誤才退回原兩組查詢；`0120_employee_directory_view.sql` 將員工清單／匯入共用的員工、機構與部門快照收斂成單一 security-invoker view，前端由 `src/lib/employee-directory-read.ts` 優先單次讀取，未套用時才 fallback 到既有快取查詢；`0121_account_directory_view.sql` 將帳號管理的帳號與角色聚合成單一 security-invoker view，前端由 `src/lib/account-directory-read.ts` 優先單次讀取，未套用時才 fallback 到既有兩組查詢；`0122_remove_redundant_master_conflict_indexes.sql` 只有在六個 named constraints 與 `apply_master_import` 均已查核後才移除 0083 遺留的重複索引，降低主檔寫入／vacuum 成本，前置條件不符時整批 fail closed。這些 migration 尚未因 Git push 自動套用到 Supabase，接手時要依既有 migration workflow 執行並做 RLS／constraint 唯讀驗證。
   - **總覽核心 read adapter**：`loadOverviewCore` 將可用量、需求、待發貨與入庫四個首屏摘要收斂成一次 view read；初次載入與庫存／工作流局部刷新都走同一個 adapter，局部刷新只投影本次 scope 需要的 JSON 欄位，未部署 0118 時的 fallback 只查本次 scope 需要的舊 view。活動歷史與稽核仍延後，權限／schema 錯誤不會被吞掉；這只改善讀取 latency，不改任何 workflow 或 mutation authority。
   - **HR 發貨明細 read adapter**：`loadWarehouseShipmentLines` 將發貨明細與需求品項快照收斂成一次 view read；0119 尚未部署才回退兩次查詢，既有 0114 未部署時的明細更新 fallback 仍保留，正式 POST／庫存鎖序／來源版本防線不變。
   - **員工目錄 read adapter**：`loadEmployeeDirectory` 將員工清單／匯入共用的員工、機構與部門快照收斂成一次 view read；同一 Supabase client 的並行／短時間重複讀取會去重並快取最多 30 秒，員工或機構／部門 mutation 成功後由 `invalidateEmployeeDirectory` 明確失效；0120 尚未部署才回退既有快取讀取，保存、停用與匯入 RPC 不變。
   - **帳號目錄 read adapter**：`loadAccountDirectory` 將帳號與已存角色聚合成一次 view read；0121 尚未部署才回退兩次查詢，帳號新增、角色／狀態／Auth 綁定仍由既有 server-side API 處理。
   - **Read-model 缺漏快取**：總覽、帳號／員工目錄、更正歷史、HR 發貨及補庫明細共用 `src/lib/read-model-rollout.ts`；每個 Supabase client 只對缺漏 view 快取 60 秒，期間略過重複失敗 request、沿用既有 legacy read，期滿自動再探測。只快取 PGRST205 或明確點名目標 view 缺漏的 missing-object error，permission／其他 schema error 仍原樣走安全錯誤處理。這僅緩解 migration 尚未到位時的額外 round trip，不取代正式 `restore-application-read-models.sql`；完整契約見 `docs/architecture/module-workbench.md` 第 38 點。
   - **總覽局部刷新**：`src/domain/overview-refresh.ts` 定義總覽讀取範圍；初次進入才讀取全部摘要，庫存異動重讀可用量、入庫進度與發放摘要，人資需求／發貨異動重讀工作佇列、可用量、發放與稽核摘要；`loadOverviewCore` 會依 scope 投影 view 欄位，同一操作同時發出兩類事件時在下一次 render 前合併，避免資料過期或重複查詢。新增 dashboard 事件時先更新 scope seam 與測試，不要把所有 view 綁在同一個 reload token。
   - **總覽核心／延後讀取**：`overviewReadPlanForScope` 讓待辦、可用量、發貨與入庫先完成可操作摘要，發放歷史與稽核活動背景補齊；前台明確顯示活動摘要載入中，不得恢復成等待所有 reporting view 完成後才解除總覽 loading。
   - **Session 更新與資料讀取解耦**：`useWorkspaceIdentity` 以穩定 Auth user id 而非旋轉中的 User／access token 物件作為 `app_accounts`／角色查詢 key；庫存、需求、倉庫、更正與 durable import panel 不得把短效 access token 放進讀取 effect dependency，仍需保留 `retrySupabaseQueriesAfterSessionRefresh` 與明確身份／workflow／手動 refresh 觸發。退回原因碼只在面板啟用時讀取一次，避免選項變更造成重複查詢。
14. **狀態與查詢欄位 seam**：`src/domain/workflow-status.ts` 是前台工作狀態的唯一中文轉譯器；PDF、ERP、員工匯入、總覽不直接顯示未知 DB code 或 raw error。`ReportingPanel` 與 `InventoryHistoryExportPanel` 依 `reporting-catalog` 的欄位白名單查詢，報表讀取並沿用共用 session retry，保留原有報表欄位／篩選／稽核語意，不使用 `select("*")`。
15. **隱藏頁籤活動控制**：`RetainedPanelSet` 保留表單狀態但透過 `PanelActivityContext` 暫停隱藏頁籤的 Supabase 查詢／事件刷新；目前 17 個保留型資料 panel 都以 `usePanelActivity()` 將 `panelActive` 納入讀取／恢復 effect 的 guard，重新切回可見時才重讀。契約測試見 `src/domain/retained-query-panel-contract.test.ts`；新增任何 panel effect 或 workflow event listener 時，不可讓保留狀態等同持續背景輪詢。
16. **工作區按需載入**：`WorkspaceShell` 透過 `next/dynamic` 載入七個 `src/app/workspaces/*Workspace.tsx`，各 workspace 再以 `next/dynamic` 拆開次要 panel；主要入口保持立即載入，未開啟的商品、庫存、匯入、報表與更正模組不進入首屏 bundle。左側工作區按鈕在 pointer 指向或鍵盤聚焦時只預取該工作區 chunk，不預先查詢資料；切換時由 workspace／panel 共用的 `WorkspacePanelLoading` 顯示同頁骨架、`aria-busy` 與 polite status，不改變 hash／module 狀態，不呈現 lazy-loading 內部實作說明，並支援 `prefers-reduced-motion`。`WorkspacePanelLoading.test.ts` 與 `workspace-loading-contract.test.ts` 驗證使用者可見載入狀態及兩層 dynamic seam；目前 build 基準 `/app` First Load JS 為 187 kB，workspace chunk 約 20–39 kB，不要因方便而恢復 workspace 或次要 panel 的靜態匯入。
17. **主檔讀取快取**：`src/lib/master-data-cache.ts` 是商品／組織／員工／採購選項讀取的唯一共用 adapter；它以登入中的 Supabase client 做請求去重與 30 秒 TTL，讀取錯誤不快取。商品、組織、員工清單／編輯器、員工匯入、換季活動、帳號管理與採購決策都沿用此 adapter，且 catalog／editor／整批主檔面板的讀取與保存入口都先通過 `useWorkspaceSession` identity readiness；採購決策的供應商／MOQ／差異原因碼由 `loadProcurementMasterData` 一次提供，商品／組織／員工穩定清單也透過單次 session refresh/retry。人資請領由 `loadHrRequestMasterData` 透過 `v_hr_request_employee_options`／`v_hr_request_item_options` 兩組 server-shaped view 直接提供有效員工與含兩倉可用量的品號，補庫、盤點、盤點更正、換季活動與 ERP 匯出再共用 `loadActiveEmployeeOptions`、`loadActiveItemOptions`、`loadActiveWarehouseOptions`、`loadActiveInstitutionOptions`，換季活動只讀在職員工的最小選項欄位，其他庫存清單仍每次從 `v_item_availability` 讀取。主檔保存／停用／匯入後，以及使用者按下任何主檔／人資／換季／採購的「重新整理」後，都會呼叫對應 invalidate 清除快取；背景重複讀取仍維持去重。新增主檔讀取時先擴充這個 adapter，不要在 panel 內重新建立相同 institutions／departments／uniform_items／suppliers／supplier_uniform_items／employees／warehouses 查詢。
18. **非同步檔案狀態輪詢**：`src/app/use-workflow-status-poll.ts` 是 PDF／ERP artifact 的共用輪詢 seam；只在可見頁籤、存在 artifact 且尚未 `READY`／`FAILED` 時以遞迴 `setTimeout` 執行，下一次請求一定等前一次完成，離開頁籤或進入終態即停止。手動重新查詢與背景 polling 共用 in-flight promise；前景查詢可在讀取中再次觸發而不增加 RPC，`READY` 成品也不因狀態讀取而鎖住下載，資料庫下載 RPC 仍重新驗證角色與狀態。互動門檻細節見 [`docs/architecture/ui-workspaces.md`](./docs/architecture/ui-workspaces.md)。`poll()` 的暫時例外必須在 hook 內安全捕捉並於下一個間隔重試，不能讓非終態 artifact 卡住或產生未處理 rejection；前景查詢才負責使用者可見錯誤。新增非同步狀態 UI 時沿用此 hook，不要各 panel 自行建立重疊 interval，也不要移除後端冪等／稽核防線。
18a. **耐久匯入輪詢**：`DurableImportPanel` 也必須使用同一 `useWorkflowStatusPoll` seam，將 Edge 觸發與批次讀回包在單次串行 poll；暫時例外交由 hook 於下一間隔重試，`VALIDATED` 代表等待使用者確認、`APPLIED`／`FAILED`／`CANCELLED` 代表終態，均不再自行建立第二套 `setTimeout` 迴圈。這只簡化前端狀態實作，不改變 upload、preview、confirm、APPLY、RLS、冪等或資料庫 lease 防線。
20. **業務 panel 讀取與錯誤 seam**：帳號、理貨、入庫、補庫、退回、盤點、採購、換季、員工、各類更正與報表 panel 的伺服器錯誤都經由安全 adapter 或固定中文提示處理；新增業務 mutation 時不可直接插入 `${error.message}`，報表與需求歷史的隱藏頁籤也必須先檢查 `usePanelActivity()`。使用者可修正的本機表單／CSV 格式錯誤才可保留具體訊息。
21. **耐久匯入狀態 seam**：`src/domain/durable-import-ui.ts` 的 `isDurableImportProcessingStatus`、`isDurableImportRowsVisibleStatus`、`isDurableImportTerminalStatus` 與 `isDurableImportAwaitingUpload` 是 `DurableImportPanel` 的唯一狀態判斷入口；不要在 panel 內重新建立字串陣列，避免處理中、等待確認與終態的 UI 行為分叉。Edge Function 按需執行後，panel 必須立即以同一批次讀回狀態；若已 `VALIDATED` 就直接顯示預覽，只有仍在處理時才保留背景輪詢。逐列 preview 只在 `VALIDATED`／`FAILED` 讀取；`APPLYING`／`APPLIED` 的輪詢只更新批次，不得每 2.5 秒重載最多 10,000 列及其差異。`summarizeDurableImportPreview` 每份 rows 快照只掃描一次，保留完整未確認差異數，只把前 10 筆顯示資料交給 UI；confirm RPC／worker APPLY 仍驗證全部資料與差異。一般檔案選取直接啟動上傳／檢查，範例檔仍由 staging 勾選控制，正式套用前的確認不可省略。取消理由屬於低頻破壞性操作，只在使用者展開「取消批次」後顯示，不得讓一般處理畫面常駐一組取消欄位。背景 Edge 輪詢使用獨立 read seam，不得讓整個面板顯示 mutation「處理中」；批次讀取不得無條件鎖住終態批次的匯入類型／建立新批次，恢復流程以 `recoveryLoading` 鎖住並顯示狀態，檔案／批次切換必須以 read generation 加 `createReadRequestController` 同時丟棄晚到的資料與 loading 回應；只有前景預覽確認仍依賴完整 rows，其他上傳／取消／確認套用使用 `busy`。
22. **PDF／ERP 恢復 seam**：`src/domain/workflow-recovery.ts` 的 `workflowRecoveryCandidates` 統一恢復順序；`PdfArtifactPanel` 與 `ErpExportPanel` 必須先以 localStorage 的 entity ID 查詢，只有查不到才用同一筆冪等鍵恢復，避免同一批次重複查詢與競態。PDF 恢復、單據清單與 PDF／ERP artifact 狀態輪詢都沿用 `retrySupabaseQueriesAfterSessionRefresh`；恢復成功後仍保留現有 ID，不能為了減少查詢而移除冪等鍵或後端結果查詢。
23. **交易下一步 seam**：`PurchaseReceiptPanel`、`ReturnPanel`、`StocktakePanel` 與五種更正面板在資料完整時都提供「確認並完成」一鍵路徑，未完成資料仍可先建立草稿；盤點優先使用 `complete_stocktake` 單一 transaction RPC，在同一交易中委派既有 create/update 與 `post_stocktake`，只用一個瀏覽器往返完成，`STALE_COUNT` 仍必須重新擷取帳面並重盤。只有 shared `src/lib/optional-rpc.ts` 明確確認 function 缺少時才 fallback，按 Supabase client／函式快取 60 秒並於期滿重探；權限錯誤、網路未知與其他 schema 錯誤不可走 fallback。回應未知時鎖住輸入、沿用同一組冪等鍵重試。`0127_atomic_stocktake_completion.sql` 必須另行套用，Git／Vercel 部署不會代替 Supabase migration。「先建立盤點草稿」、「保存盤點草稿」與 `0113` 重盤入口仍保留。`WarehouseShipmentPanel` 只有在理貨明細真的有未保存變更時才阻止切換需求或重新整理，已保存草稿可直接切換、恢復與刷新清單。採購入庫的採購單來源不因明細查詢而鎖住，只有交易忙碌或已有入庫草稿時才停用，明細選擇與送出仍受 loading／資料完整性保護。`0114_warehouse_post_with_lines.sql` 套用後，HR 發貨的明細更新與過帳由同一個伺服器 transaction seam 完成，不再由瀏覽器逐筆 UPDATE；未套用時才 fallback。所有交易在成功 POSTED 後提供同頁「處理下一筆／建立下一張盤點」，完成紀錄仍 immutable，按鈕只重設本地表單、清除 operation key 並回到選擇狀態，盤點保留倉別並產生新的單號。新增庫存交易 panel 時，必須提供同等的完成後續作入口，不要求使用者切換頁籤或整頁重整；`warehouse-operation-contract.test.ts` 要同步保護這個 UI 契約。
23a. **盤點重盤 transaction seam**：`0113_stocktake_server_recapture.sql` 提供 `recapture_stocktake_draft`，由資料庫在同一交易內從既有盤點明細推導品號、鎖定餘額，再呼叫既有 `update_stocktake_draft` 完成重盤確認並回傳最新明細；`StocktakePanel` 優先使用此 RPC，若目標 project 尚未套用 0113 才 fallback 到舊的瀏覽器餘額讀取。缺漏偵測與一分鐘負快取共用 `src/lib/optional-rpc.ts`，不再另存 panel-local 支援旗標。這只減少前端往返與讀寫競態，不放寬 `STALE_COUNT`、角色、冪等鍵、品號鎖或 balance version fencing。套用 migration 後仍需以 HR／WAREHOUSE session 做一次重盤與結果未知重試驗證。
23b. **發貨 transaction seam**：`0114_warehouse_post_with_lines.sql` 提供 `post_warehouse_shipment_with_lines`，先在同一交易內驗證完整明細集合、保存實際調庫量，再委派既有 `post_warehouse_shipment` 完成庫存與需求狀態變更；`WarehouseShipmentPanel` 優先使用此 RPC，只有目標 project 明確尚未套用 0114 才回退。相容路徑只更新相對已保存草稿確有變動的列，並以 `UPDATE ... RETURNING id` 確認資料庫實際寫入後才 POST；新建但未改動的理貨草稿不再逐列重寫。能力判定共用 `src/lib/optional-rpc.ts`，同時辨識 PGRST202／42883 並按 client 快取 60 秒。收到結果後必須比對單據 ID 與 `POSTED`／`SHIPPED` 終態才顯示成功；傳輸例外或回應形狀不完整時保持同一冪等鍵供重試，並解除畫面 busy 狀態。這只減少往返與半完成狀態，不放寬 WAREHOUSE 角色、來源版本、預留、庫存鎖序或過帳防線。
24. **換季讀取恢復 seam**：四個換季 panel 的手動重新載入只受 mutation `busy` 控制，不得因 `dataLoading`／`campaignLoading`／`scopeLoading` 禁用，讓使用者能在卡住或暫時同步時自助重試；直接查詢使用 `retrySupabaseQueriesAfterSessionRefresh`，活動選項使用既有 `master-data-cache` adapter。重新載入以 `reloadToken` 觸發，保留目前活動／需求列／核准品項，不得因暫時 JWT 不同步而清空工作區或另寫 `auth.refreshSession()`；`SeasonalCampaignPanel` 以帳號綁定的員工／品號快照判斷建立邊界，已凍結範圍的活動不因背景選項重讀而阻塞開放；`SeasonalDemandPanel` 以帳號／活動的 campaign 與 scope 快照判斷依賴，只有活動切換、初次尚未取得範圍或非暫時權限／Schema 錯誤才鎖定員工／品號與送出，數量／備註／清除表單不可被背景讀取整段鎖住，且以讀取序號丟棄舊回應；`SeasonalProcurementPanel` 的 queue／master-data 讀取必須以 `createReadRequestController` fencing，`SeasonalApprovalPanel` 的 queue 與明細各有獨立 controller，重新載入或切換選取先 invalidate 舊 token，舊回應不得發布 rows、重設編輯器或關閉新讀取的 loading；契約由 `seasonal-read-path-contract.test.ts` 保護。
25. **總覽讀取恢復 seam**：`OverviewDashboard` 的六個 reporting view 仍依 `overviewReadPlanForScope` 分成核心／延後兩組；四個核心摘要在初次載入與事件刷新都使用 `loadOverviewCore`，0118 尚未部署時只 fallback 目前 scope 所需的舊 view，活動／稽核仍以共用 session retry 背景補齊。面板內「重新整理總覽」與 `mergeOverviewReloadScope` 必須保留，不得改回整頁刷新或讓低優先活動 view 阻塞核心摘要。
26. **恢復讀取邊界**：帳號管理的帳號／角色／範圍清單、員工匯入錯誤列、ERP 批次恢復、採購差異原因碼，以及人資／盤點／調撥更正的狀態與來源恢復，都必須經由 `retrySupabaseQueriesAfterSessionRefresh`。這只修復短暫 JWT 不同步，不會把權限／schema 錯誤當成可重試；原因碼面板另以卸載旗標避免延遲結果回寫已離開的頁籤。契約由 `read-retry-boundary-contract.test.ts` 保護。
27. **管理清單檢視設定**：`ManagementCatalogTable` 仍保留每頁筆數、密度、欄位顯示、排序與分頁，但日常工具列只顯示資料筆數；非日常的三組檢視控制收進預設收合「顯示設定」，避免每個模組首屏常駐過多按鈕。共用表格另以 `loading` seam 區分「正在查詢」與「查詢後為空」，已有資料重新整理時保留舊列並標記 `aria-busy`；載入骨架支援 `aria-live` 與 `prefers-reduced-motion`。換季、原因碼、商品、組織、員工、換季活動、退回與盤點呼叫端另以 `dataLoading` 和 `busy` 分別表達讀取／提交；必要的依賴選項載入時只停用相關選擇與送出，退回若已有有效原因碼則不因原因碼背景重讀而鎖住交易動作，代碼、名稱、日期與備註等不相關欄位仍可先填。盤點已有同倉別主檔／帳面快照時，背景重讀只更新狀態，不鎖住既有表單；只有首次尚未取得快照、切換新倉別或權限／Schema 失敗才停止依賴選擇與新增／送出，並保留伺服器 RPC 的版本 fencing。這只改顯示層，不改查詢集合、匯出集合、RLS 或欄位白名單；契約由 `management-catalog-ui-contract.test.ts`、`master-data-editor-ui-contract.test.ts`、`seasonal-read-path-contract.test.ts`、`warehouse-operation-contract.test.ts` 保護。
28. **交易操作列**：`WorkflowActionBar` 統一人資需求、入庫、退回、盤點、發貨、五種更正、PDF 與 ERP 面板的主要下一步；修改本張、取消、暫存草稿、保存修改、重盤、查詢狀態、另建與下一筆等低頻操作收進「其他操作」，但仍沿用原本的 RPC、冪等鍵、版本 fencing、輪詢、RLS 與 append-only 過帳。新增交易或非同步產物面板時不要自行並排重複操作列；契約由 `workflow-action-bar-contract.test.ts` 保護。
29. **帳號管理按需掛載**：`AccountAdminPanel` 的帳號清單／新增／設定分頁改由 `RetainedPanelSet` 管理；未訪問的表格與大型表單不再以 `hidden` 同時留在 DOM，已訪問分頁仍保留未送出狀態。新增管理工作台分頁時，優先使用同一 mount seam，不能以隱藏 DOM 取代按需掛載；契約由 `account-admin-workbench-contract.test.ts` 保護。
30. **帳號管理延後讀取**：`AccountAdminPanel` 首次進入只查 `app_accounts`／`user_roles`；`coordinator_scopes` 與啟用中的機構／部門選項只在選取帳號、進入「帳號設定」或手動重新整理設定時載入。兩組設定讀取各自追蹤狀態並共用遞增序號；已有選項快照時，背景選項刷新不鎖住選擇或窗口操作，窗口保存只等目前帳號的 scope 讀取完成，不必等待無關的機構／部門刷新；首次尚無有效選項時才停用相依選擇與保存。密碼、角色、狀態、Auth 綁定與刪除不依賴這組低頻主檔，不能把低頻設定查詢放回帳號清單首屏；目錄重新整理只受 `directoryLoading` 控制，並且 scope／選項舊帳號回應均由序號丟棄；契約由 `account-admin-workbench-contract.test.ts` 保護。
30a. **換季與 ERP 讀取鎖**：`SeasonalCampaignPanel` 首次載入才預設全選活動範圍，手動重新整理只保留目前仍有效的員工／品號選取，並以讀取序號丟棄過期回應；同一帳號已有選項快照時，暫時 JWT 不同步只顯示背景同步提示，不清空範圍或鎖住活動基本欄位；切換帳號、初次尚未取得快照或權限／Schema 失敗才清除選項並禁止建立。已完成範圍凍結的活動可在背景選項重讀時直接開放窗口，伺服器 RPC 仍是最後驗證邊界。`ErpExportPanel` 將機構選項與 artifact 狀態拆成 `institutionLoading`／`artifactLoading`，讀取機構時發放日期仍可編輯，artifact 查詢不會再把建立批次誤鎖成同一個 loading 狀態。兩者都不得以舊的單一 `dataLoading` 恢復整段表單鎖定；契約由 `seasonal-read-path-contract.test.ts` 與 `read-refresh-ui-contract.test.ts` 保護。
30b. **PDF／ERP artifact 刷新與下一步**：背景輪詢和手動狀態查詢共用同一個 in-flight request；READY artifact 可下載，ERP 的 FAILED 可同批重試，READY／FAILED 都可在唯讀刷新期間開始下一份。只有 PREPARING 等非終態工作才禁止另開同類 revision。開始下一份／同批重試會以 `createReadRequestController` 作廢舊狀態查詢，並以目前 artifact id 丟棄晚到輪詢結果，避免恢復舊 artifact 或清除新操作的 loading；終態判斷集中在 `isArtifactTerminalStatus`。下載 RPC 仍在資料庫重新驗證角色與狀態；契約由 `workflow-polling-contract.test.ts`、`workflow-status.test.ts` 與 `read-refresh.test.ts` 保護。
31. **倉庫清單讀取 seam**：`0103_warehouse_shipment_queue_view.sql` 以 `security_invoker` view 合併 `SUBMITTED` 人資需求、`SUBMITTED` 補庫單與 DRAFT 發貨草稿；`WarehouseShipmentPanel` 透過 `src/lib/warehouse-shipment-queue-read.ts` 讀 view，未部署時只對明確 missing-view 使用受 RLS 保護的來源表 fallback，優先以發貨 FK embedded read，只有 PostgREST relationship metadata 缺漏才分開查 DRAFT。權限與其他 schema 錯誤不 fallback，任一來源失敗就清空整份 queue，避免呈現部分清單。此 adapter 把資料讀取移出 panel、保留 view 上線後的單查詢路徑；明細、草稿、冪等、庫存鎖序與 POST RPC 維持原契約。套用 migration 後仍須用 HR／WAREHOUSE session 做 RLS 唯讀驗證，contract tests 保護 rollout seam。
32. **人資請領選項 seam**：`0104_hr_request_option_views.sql` 以兩個 `security_invoker` view 組合有效員工／隸屬與品號可用量；兩個 view 都存在時，`loadHrRequestMasterData` 僅需兩組查詢。若 PostgREST 明確以 `PGRST205`／`42P01` 指出指定 view 不存在，才使用既有 RLS 保護的 employees／institutions／departments 與 `v_item_availability` fallback，並以 30 秒負向 capability cache 避免反覆查不存在的 view；權限錯誤不 fallback，仍安全拒絕載入。唯讀查核曾確認目標正式 DB 尚無兩個 0104 view，因此程式 fallback 是當前可用路徑；套用 0104 可恢復兩組查詢的最佳化，仍須另做 HR session RLS 驗證。送出 RPC 持續重新鎖定／重算，契約由 `master-data-cache.test.ts` 保護。
33. **調撥更正來源 seam**：`0105_warehouse_transfer_correction_source_view.sql` 以 `v_warehouse_transfer_correction_sources` 將已完成發貨／補庫的來源單與明細整理為單一唯讀 catalog；`WarehouseTransferCorrectionPanel` 只查這個 view，不要恢復四組來源／明細查詢或在瀏覽器以 `find()` 合併。view 只改善初始選項讀取，建立／過帳 RPC、品號鎖定、更正歷史與 append-only 規則維持原契約；套用 migration 後仍須用 WAREHOUSE session 做 RLS 唯讀驗證。
33. **調撥更正來源 seam**：`0105_warehouse_transfer_correction_source_view.sql` 以 `v_warehouse_transfer_correction_sources` 將已完成發貨／補庫的來源單與明細整理為單一唯讀 catalog；`WarehouseTransferCorrectionPanel` 只查這個 view，不要恢復四組來源／明細查詢或在瀏覽器以 `find()` 合併。view 只改善初始選項讀取，建立／過帳 RPC、品號鎖定、更正歷史與 append-only 規則維持原契約；套用 migration 後仍須用 WAREHOUSE session 做 RLS 唯讀驗證。
34. **盤點更正來源 seam**：`0106_stocktake_correction_source_view.sql` 以 `v_stocktake_correction_sources` 將啟用倉庫的已完成盤點與明細整理為單一唯讀 catalog；`StocktakeCorrectionPanel` 只查這個 view，再依已展開的 HR／WAREHOUSE capability 過濾倉別。不要恢復分段查盤點單、倉庫與明細，也不要把盤點過帳、帳面版本 fencing 或更正 RPC 搬進 view。
35. **入庫／退回更正來源 seam**：`0107_receipt_return_correction_source_views.sql` 以 `v_purchase_receipt_correction_sources`／`v_return_correction_sources` 分別整理已完成主單與明細；兩個更正 panel 只查對應 view，不要恢復先查 header 再查 lines 的瀏覽器流程。view 只改善初始選項讀取，更正草稿、過帳、歷史與不可變交易規則仍由既有 RPC／資料表負責。
36. **人資發放更正來源 seam**：`0108_hr_issue_correction_source_view.sql` 以 `v_hr_issue_correction_sources` 整理已發放需求與發放明細；`HrIssueCorrectionPanel` 只查這個 view，不要恢復先查 `hr_requests` 再查 `hr_issue_lines` 或在選單 render 中以 `find()` 回查主單。view 只改善讀取 locality，正式更正 RPC 仍重新驗證來源、數量、庫存與稽核。
37. **換季採購 queue seam**：`0109_seasonal_procurement_queue_view.sql` 以 `v_seasonal_procurement_queue` 將 APPROVED 品項與既有 `seasonal_procurement_lines` 左連接成一個 queue；`SeasonalProcurementPanel` 不要恢復兩次來源查詢或以第二份狀態回配，也不要在清單讀取完成時自動選第一筆。供應商／MOQ／差異理由碼仍由 `loadProcurementMasterData` 快取，保存決策與建立採購單仍只能走原 RPC；目前選取失效時要清空右側編輯狀態。
38. **換季需求 workspace seam**：`0110_seasonal_demand_workspace_view.sql` 以 `scope_kind` 標記的 `UNION ALL` 一次提供活動員工、活動品號與既有需求；`SeasonalDemandPanel` 只查 `v_seasonal_demand_workspace` 後在 adapter 分流，避免三組查詢與笛卡兒積。保存仍只能走 `upsert_seasonal_demand_line`，不可把需求 mutation、授權或冪等規則搬進 view。
39. **換季審核 queue seam**：`0111_seasonal_approval_queue_view.sql` 將 PENDING submission 與活動編號／名稱整理成單一 security-invoker view；`SeasonalApprovalPanel` 不要恢復兩次清單查詢或由瀏覽器 `find()` 回配，不要在首次／刷新時自動選第一筆，審核明細仍選取後載入，核准／退回仍只能走既有 revision、snapshot hash 與 RPC。
40. **人資需求 detail seam**：`0112_hr_request_history_detail_view.sql` 以 `detail_kind` 標記的 `UNION ALL` 一次提供品號彙總、員工發放與預留紀錄；`HrRequestHistoryPanel` 只查 `v_hr_request_history_detail` 後分流，避免三組明細查詢。這是唯讀 read seam，不得把需求 mutation、預留或發貨規則搬進 view。

41. **換季活動原子建立**：`SeasonalCampaignPanel` 使用 `runSeasonalCampaignSetup` 優先以 `create_seasonal_campaign_with_scope` 單次 RPC 建立活動並凍結員工／品號範圍；`0125_atomic_seasonal_campaign_setup.sql` 在一個資料庫 transaction 內委派既有 create／scope RPC，保留角色檢查、獨立冪等鍵與稽核，任一步失敗即回滾。只有 PostgREST 明確回報新 RPC 不存在時才退回舊兩步流程；舊流程部分成功會保留 campaign ID，只重試 scope，不會重複建立。套用 0125 前不會得到單一往返最佳化，Git／Vercel 部署也不會代替 Supabase migration；契約由 `seasonal-campaign.test.ts` 與 `seasonal-campaign-contract.test.ts` 保護。
42. **採購入庫原子完成**：`PurchaseReceiptPanel` 的完整「確認並完成入庫」優先呼叫 `complete_purchase_receipt` 單一 RPC；`0126_atomic_purchase_receipt_completion.sql` 使用 `SECURITY DEFINER`，先明確檢查 authenticated／WAREHOUSE，再預鎖品號，依既有 item→PO 鎖序於同一交易委派 `create_purchase_receipt_draft`／`post_purchase_receipt`，保留兩組冪等鍵、稽核與 POSTED 邊界。只有共用 `src/lib/optional-rpc.ts` 確認函式缺少時才使用舊雙 RPC 相容流程；明確缺漏按 Supabase client／function 快取 60 秒，未知、權限與業務錯誤不轉成 fallback。若舊流程 POST 失敗或回應遺失，保留 DRAFT、冪等鍵與相同 request fingerprint 以便重試。未完整分類的到貨仍可先保存草稿；套用 0126 前不會得到單次往返最佳化，Git／Vercel 部署不會代替 Supabase migration；契約由 `purchase-receipt-completion.test.ts` 與 `warehouse-operation-contract.test.ts` 保護。
43. **五種庫存更正原子完成**：退回、人資發放、採購入庫、盤點與倉庫調撥更正的一鍵完成優先呼叫 `complete_*_correction`；`0131_atomic_correction_completion.sql` 的 `SECURITY INVOKER` wrapper 只在同一 DB transaction 委派既有 create／post RPC，不直接寫資料表，不改角色、鎖定、稽核或冪等規則。`src/domain/correction-completion.ts` 僅在共用 capability adapter 明確確認指定 wrapper 不存在時才回退兩個既有 RPC；60 秒負快取共用並允許期滿重探，未知結果沿用同一組冪等鍵。POST request fingerprint 僅含操作碼與冪等鍵、不存表單內容，並在草稿重試時保持一致；結果查無既有操作則清除舊操作鍵並解除鎖定。手動「先建立草稿」仍保留；`0131` 須另依 Supabase workflow 套用與 staging 驗證，Git／Vercel 部署不會更新 DB。契約由 `correction-completion.test.ts`、`correction-completion-sql-contract.test.ts`、`correction-completion-contract.test.ts` 與 `warehouse-operation-contract.test.ts` 保護。

44. **共用 optional RPC 能力快取**：`src/lib/optional-rpc.ts` 集中可選 transaction wrapper 的缺漏判斷，收貨、退回、盤點／重盤、人資需求、補庫、換季活動、五種更正與倉庫發貨共用。僅 `PGRST202` 明確指出目標函式不在 schema cache，或 `42883` 明確指出目標函式不存在時，按 Supabase client 與函式名快取 60 秒；期滿重探。網路 rejection、permission／business error、缺少 helper／dependency 或錯誤 response shape 不得變成 legacy fallback；每個業務 module 仍持有自己的 idempotency／recovery 邏輯。此 seam 移除了重複 React boolean／ref capability gates；由 `optional-rpc.test.ts` 與各 workflow domain tests 驗證。

本快照的讀取路徑 migration 範圍為 `0098`–`0112`（共十五個），另包含 SSO email 索引 `0115_sso_account_email_read_index.sql`、更正歷史彙總 view `0116_correction_history_view.sql` 與補庫發貨 read view `0117_replenishment_shipment_lines_view.sql`；交易重盤的 `0113_stocktake_server_recapture.sql` 與發貨的 `0114_warehouse_post_with_lines.sql` 是獨立的 mutation seam，不列入唯讀路徑數量。上述較早的讀取路徑總覽若仍列舊數量，應以本行與實際 migration 檔案為準。

- **補庫發貨 read seam**：`0117_replenishment_shipment_lines_view.sql` 將補庫明細與 GENERAL 可用量收斂成單一 security-invoker view，`src/lib/replenishment-shipment-read.ts` 優先單次讀取，未套用 migration 時才 fallback 到原兩次讀取；不要把該 view 當成 POST 的庫存權威，最終數量與鎖定仍由 `post_replenishment_request_with_lines` 決定。契約與 adapter tests 見 `warehouse-operation-contract.test.ts`、`replenishment-shipment-read.test.ts`。

### 下一位 agent 的工作界線

目前沒有已知還能單靠 repository code 合理宣告完成的 production gate。除非使用者提供 staging／production credentials、真實樣本、核准資料或明確要求部署／驗收，下一步應針對新的功能需求修改，而不是自行製造 PASS evidence。

正式狀態必須維持 **`NOT_READY`**，直到至少取得：第一批 production accounts／roles／demand scopes、GitHub／Vercel owner 與 backup owner、鼎新正式 mapping＋成功 import sample、正式 PDF layout/style approval、真實 Supabase migration／六角色 RLS／Auth／private Storage／signed URL／concurrency smoke、durable import Edge／worker staging integration、renderer/PDF/ERP staging smoke、Storage destructive cleanup smoke、DB 90-day staging retention destructive smoke、外部 monitoring/alerts/incident smoke、production-sized DB＋Auth＋Storage restore、量測 RPO/RTO、三年 synthetic/staging capacity evidence 與 production cutover approval。

需要繼續部署／驗收時，以 [`docs/deployment/runbook.md`](./docs/deployment/runbook.md)、[`docs/deployment/staging-preflight.md`](./docs/deployment/staging-preflight.md) 與 [`docs/implementation/roadmap.md`](./docs/implementation/roadmap.md) 為準；真實 secrets／evidence 放在受保護且 Git ignored 的位置，不要提交到 repository。

### 正式 UI 與功能 seam

- `src/app/WorkspaceShell.tsx`：登入 gate、session refresh、七個 workspace、hash 導航、共用 topbar、responsive shell，以及保留原左側導航的 `initialSystemGuide` 內嵌模式。
- `src/app/workspaces/workspace-config.ts`：workspace 定義、功能頁籤與搜尋索引；目前 workspace 為總覽、帳號管理、人資需求、倉庫作業、採購與入庫、換季活動、報表。
- `src/app/workspaces/*Workspace.tsx`：每個 workspace 的模組組裝；頁籤使用 `activeModule` 切換內容，維持在同一工作區，不用按鈕觸發頁面下移。
- 商品與庫存模組：`src/app/ProductManagementPanel.tsx` 組合商品清單、單筆新增／修改／停用／刪除、主檔小批次匯入／匯出與耐久匯入；`src/app/InventoryManagementPanel.tsx` 組合兩倉可用量、期初匯入、庫存異動入口、庫存／流水 CSV 匯出與規則試算。`ProductMasterEditorPanel` 的新增、修改、停用仍透過 `apply_master_import`，硬刪除改走 `delete_uniform_item`；該 RPC 會拒絕任何庫存餘額／庫存流水紀錄，其他業務外鍵關聯也會阻擋，並在刪除前留下 immutable audit snapshot。`InventoryOperationHub` 只導向既有期初／發貨／盤點／入庫／更正 seam；商品數量與庫存數字仍分別以既有主檔 RPC、`v_item_availability` 與庫存交易來源為準。`0080_master_data_system_admin_access.sql` 套用後，SYSTEM_ADMIN 可使用同一主檔 RPC 維護商品與供應商資料；`0081_jsonb_object_length_compatibility.sql` 必須一併套用，讓既有主檔 RPC 的 JSONB 物件欄位上限檢查使用 private compatibility bridge；`0082_master_data_item_code_qualification.sql` 必須再套用，讓既有主檔 RPC 的同名欄位以變數優先規則解析；`0083_master_data_conflict_targets.sql` 補回 `operation_commands`、機構、部門、商品、供應商與供應商品號／MOQ 的完整唯一索引，讓所有既有 `ON CONFLICT` 目標可被 PostgreSQL 推導；`0085_uniform_item_delete.sql` 提供商品硬刪除 RPC；`0087_department_parent_resolution.sql` 讓部門表單以目前機構 UUID＋代碼解析父機構，並在解析失敗時拒絕寫入 null `institution_id`。若 0083 索引、RPC 定義與 trigger 查核均通過但登入後仍回傳 42P10，先執行 `supabase/manual/rebuild-master-import-rpc.sql`，仍失敗再執行 `supabase/manual/0084_master_data_constraint_targets.sql`，將 RPC 改用已存在的 named constraints。
- 倉庫發貨模組：`src/app/WarehouseShipmentPanel.tsx` 將已送出的人資需求、既有 DRAFT 發貨草稿與 `SUBMITTED` 補庫單組合成左側待處理清單，支援需求／補庫單／日期／草稿狀態搜尋、排序、欄位顯示、密度、分頁與選取；人資需求若尚未有草稿，主要操作會先以既有冪等建立草稿並載入明細，再直接呼叫 `post_warehouse_shipment` 完成，仍保留單獨建立草稿供先核對／修改；補庫單則直接載入補庫明細與總倉上限，送出後可在同一面板開始下一筆。`src/domain/warehouse-shipment.ts` 只負責清單搜尋／排序；正式人資發貨仍走 `post_warehouse_shipment`，補庫過帳走 `0096_replenishment_post_ui.sql` 的 `post_replenishment_request_with_lines`，`0097_replenishment_post_lock_order.sql` 讓它先鎖品號再鎖來源單，瀏覽器不得直接修改已送出的補庫明細。盤點與盤點更正 UI 也依有效角色只顯示可操作的 HR／GENERAL 倉庫，避免先選到資料庫必拒絕的倉別。
- 人資需求工作台：`src/app/HrRequestWorkbench.tsx` 與 `src/app/ReplenishmentPanel.tsx` 的建立、修改、送出與取消都必須使用固定 operation／idempotency key，正式草稿修改、送出後發貨前修改與取消由 `0089_hr_request_workflow.sql` 的受保護 RPC 執行；草稿修改只允許建立者，送出後修改會重新鎖定品號、重算預留並使舊倉庫草稿因來源版本失效，取消會保留單據與明細歷史，並釋放可取消人資需求的 ACTIVE reservation。新建／DRAFT 送出優先走 `submitHrRequestOperation`／`submit_hr_request_with_lines` 單次 transaction，未知結果須以相同 payload 與 key 重試；介面與 migration 契約見 [`docs/architecture/module-workbench.md`](./docs/architecture/module-workbench.md) 第 37 點。已送出修改仍走既有 `update_hr_request`。送出後前端提供同頁「建立下一筆需求」重設入口，不修改已送出單據。`HrRequestHistoryPanel` 提供 HR／倉庫依單號、備註、狀態查詢需求單，並可查看員工明細、品號彙總、有效預留與發貨狀態；`hr-request-events.ts` 讓送出／修改／取消／POST 後的需求查詢、待發貨清單與兩倉庫存自動同步刷新。`HrRequestWorkbench` 只可列出在職員工且其機構／部門均啟用並正確隸屬；`0090_employee_parent_active_guard.sql` 另禁止停用仍被在職員工引用的機構／部門，`0092_hr_request_active_master_guard.sql` 在 `DRAFT → SUBMITTED` 狀態轉移時再做同一防線。不要恢復瀏覽器直接寫入需求表，也不要對已發貨／已過帳單據提供刪除語意。
- `0091_hr_request_submit_alias_fix.sql` 修正 `submit_hr_request` 的 PostgreSQL `UPDATE ... FROM` 別名作用域；正式函式已查核具備 `cross join` 修正，但正式環境仍重現 `invalid reference to FROM-clause entry for table "l"` 時，必須再套用 `0094_hr_request_submit_scope_hardening.sql`。0094 將快照更新改成獨立來源子查詢＋主鍵更新，並把送出驗證 trigger 與送出 RPC 的短別名全部改成描述性別名，避免同類作用域問題。
- `0092_hr_request_active_master_guard.sql` 已套用目前正式 Supabase project；唯讀查核確認 `hr_request_active_master_guard` 相關防線、0093 的 `hr_request_cancel_draft_shipment_cleanup` trigger 與 0094 的送出 scope 修正均存在。倉庫 POST 與人資取消的前端重試會保留同一冪等鍵，避免結果未知時產生第二次業務命令；取消或送出後修改只清理未過帳的 DRAFT，已 POST 的發貨單不受影響。
- 正式站已以一筆測試交易驗證「建立需求→查詢需求／預留→建立發貨草稿→POST→查詢歷史→兩倉庫存讀回」：需求進入 `SHIPPED`、發貨進入 `POSTED`、有效預留歸零、待發貨清單歸零，庫存 UI 與資料庫讀回一致。不要以此筆交易再建立第二張需求；退回／更正僅完成可用來源的唯讀驗證，需另有明確授權才執行正式過帳。
- `supabase/manual/verify-hr-request-flow.sql` 提供人資請領建立、查詢、預留、倉庫發貨與庫存過帳的唯讀一致性查核；所有 `violation_count` 應為 0，且 0094 的三個 scope flags 應為 `true`。
- 換季需求模組：`src/app/SeasonalDemandPanel.tsx` 將需求窗口的既有 `seasonal_demand_lines` 放在左側共用管理清單，右側保留活動／員工／品號／數量／備註工作區；`src/domain/seasonal-demand.ts` 只負責清單搜尋與排序。按列修改只回填表單，切換員工或品號即回到新增模式；保存仍只走 `upsert_seasonal_demand_line`，不直接修改或刪除需求資料。
- 管理清單 seam：`ManagementCatalogTable` 只接收已篩選排序的 rows、欄位定義及 page／sort callbacks；呼叫端保留查詢與 mutation。`management-catalog.ts` 的純 interface 必須固定 page clamp、核准 page size、固定欄位與至少一欄可見；CEO 待核版本則由 `seasonal-approval.ts` 提供活動／revision／送核時間／hash 的純搜尋與排序，換季採購決策品項由 `seasonal-procurement.ts` 提供品號／品名／數量／決策狀態的純搜尋與排序，換季需求登記由 `seasonal-demand.ts` 提供員工／品號／數量／更新時間的純搜尋與排序，待發貨需求由 `warehouse-shipment.ts` 提供需求單／日期／來源版本的純搜尋與排序。`CorrectionHistoryTable`／`correction-history.ts` 只組合五個更正來源的唯讀歷史，不得承擔過帳或直接資料異動。帳號清單只選取資料，異動仍走 `/api/admin/accounts`；兩倉庫存只讀 `v_item_availability`；營運報表只讀 security-invoker views；原因碼保存只走 `maintain_procurement_difference_reason`；CEO 核准仍只走 revision/hash 重驗的 `review_seasonal_submission`，採購決策仍只走 `set_seasonal_procurement_line`；需求登記清單只讀目前活動可見列，選取後由原 panel 回填，保存仍只走 `upsert_seasonal_demand_line`；待發貨清單只讀已送出需求與既有 DRAFT，建立與 POST 仍走原有 RPC／RLS。
- `src/app/WorkspaceTopbar.tsx`：搜尋、通知、日期／資料狀態、帳號操作與風格下拉選單。
- System Guide seam：`src/app/system-guide/page.tsx` 只選擇 `WorkspaceShell initialSystemGuide`；`src/app/system-guide/SystemGuidePageClient.tsx` 顯示右側文件，`src/app/use-system-guide-access.ts` 負責入口顯示與背景預取，`src/app/api/system-guide/route.ts` 與 `src/server/system-guide.ts` 負責最終授權與讀檔。文件檔名、標題與讀者的唯一 allowlist 在 `src/domain/system-guide.ts`。
- `src/app/AccountAdminPanel.tsx`、`src/app/api/admin/accounts/route.ts`、`src/server/account-admin.ts`：帳號管理模組。2–50 個小寫英數字的登入帳號與至少 6 字元密碼必填，聯絡 Email 選填且不作登入；建立或編輯時可一次管理六角色，保存後重新查核 `app_accounts`／`user_roles`，並支援需求窗口機構／部門範圍、啟用／停用、Auth 綁定重設／解除與保留業務歷史的刪除。所有操作透過 server-side API、idempotency key、SYSTEM_ADMIN 防線與稽核理由處理；`0088_system_admin_all_roles.sql` 套用後 SYSTEM_ADMIN 具備所有角色的有效權限。
- `src/app/use-appearance-theme.ts` 與 `src/app/globals.css`：外觀 adapter seam。下拉選項為 `AP`、`MX`、`GS`、`MB`、`SH`；`GS` 的顯示名稱是 GS，但程式 id／`data-appearance` 仍是 `ga`，不要只為改名而破壞既有 localStorage 或 motion 判斷。GS motion 由 CSS compositor 動畫處理，不在工作區主路徑載入動畫 runtime；`SH` 是 shadcn/ui-inspired neutral token 版本，未加入 shadcn runtime dependency。
- SSO 已驗證工作入口：`src/server/target-session.ts` 以 server-side Supabase `auth.getUser()` 驗證 cookie；根頁導向 `/app`，`/app` 無有效 Session 導向 `/login`，已有 Session 的 `/login` 直接導向 `/app`，不把已驗證使用者留在帳密表單。一般 SSO callback 先由 `src/app/api/sso-login/route.ts` 將 ticket 寫入短效 server-side HttpOnly state，303 導向同源 `/login?sso_pending=1`；`src/app/SsoLoginPrompt.tsx` 顯示「SSO帳號登入中」確認視窗，只有「繼續 SSO 登入」才回 callback 讀取 HttpOnly ticket、驗票、建立目標 Session 並導向 `/app`，取消只清理 pending state。pending state 缺失、過期或格式不完整時顯示 allowlisted `sso_pending_state_missing` 並回到原有登入；提示視窗包含淡入／spinner、`aria-live` 與 reduced-motion 支援。`sso_flow=account_binding` 仍保留原登入表單並顯示「SSO帳號綁定中」。確認後仍由 `src/server/sso-adapter.ts` 在 server-side 以 Admin `generateLink` 的短期 hash 呼叫 target `verifyOtp`，不把 magic-link action URL 或 token 導向瀏覽器。`/auth/callback` 僅保留給既有非 SSO implicit magic-link，明確拒絕 PKCE code／混合 flow，並在成功或失敗後以 `history.replaceState` 清除 callback URL。帳號綁定成功也導向 `/app`。公開 `GET /api/sso-health` 維持 200 `{ ok: true }`；`/api/sso-login` 仍只在 server-side 呼叫中央 verify／binding API，不新增 Secret。正式環境須把 `https://uniform-co.vercel.app/auth/callback` 加入 Supabase Auth Redirect URLs，並用 Portal 新 ticket 做登入／綁定後瀏覽器工作入口驗收。
- SSO 首屏維護：`src/app/login/loading.tsx` 與 `src/app/app/loading.tsx` 必須維持可立即渲染的 `SsoLoadingShell`，不能讓登入驗證、工作區資料查詢或 JS hydration 造成白頁；骨架動畫只使用 opacity／transform，並保留 `aria-busy`、`aria-live` 與 reduced-motion。`SsoLoginPrompt.tsx` 的確認請求必須以 `Accept: application/json` 取得同源成功導向資料，不可使用瀏覽器遮罩的 `redirect: manual`；同時必須相容舊版 303，透過 Fetch 正常 follow 後驗證 `response.redirected`／`response.url`，再進入同源 `/app`。瀏覽器回應、網路與 redirect 錯誤必須使用明確 allowlist 階段（`sso_configuration_failed`、`sso_callback_failed`、`sso_response_invalid`、`sso_client_request_failed`、`sso_redirect_invalid` 等），不得恢復 `unexpected_error`。

### 正式應用接手規則

1. 登入、Auth Admin、角色、範圍與帳號異動不可從瀏覽器繞過 server-side API／受保護 RPC；不要把 Supabase service role 放入 client bundle。
2. 視覺需求先沿用 theme adapter seam；共用 base CSS 只放所有風格都需要的規則，單一風格放在 `html[data-appearance="..."]` scope 內。
3. 功能需求先找對應 workspace、panel、domain function 與 migration，再做最小 patch；不要用 prototype 的記憶體 mock 代替正式資料流。
4. 新增可見欄位時同步檢查資料模型、表單、列表、詳情、篩選、RPC、RLS 與測試；新增需求狀態時同步檢查 `advanceRecord()`、`rejectRecord()`、`syncRequestTask()`。
5. 本機與 CI 驗證使用 Node/npm 指令；本文件的範例以 `cmd.exe`／一般 shell 可執行，不依賴 PowerShell。
6. Storage cleanup 的 eligibility 只能由 `0071_storage_cleanup_contract.sql`／`0072_import_terminal_retention.sql` 的 DB RPC 決定；runner 只能做第二層 bucket/key sanity check。不要在 script／workflow 自行推論可刪狀態、放寬 24 小時下限或 FAILED／CANCELLED import 的 90 天 terminal retention、重設 renderer/import lease，或刪除 artifact／import／audit evidence row。
7. DB staging payload retention 只走 `0074_import_staging_payload_retention.sql` 的 `job_import_retention` 薄 RPC。它只 scrub FAILED／CANCELLED 滿 90 天 batch 的 `raw_values`／`normalized_values`／`validation_errors`，保留 batch/row/chunk/diff evidence 與 `staging_purged_at`；purge 後不可 restart。不要把 `job_storage_cleanup` 擴權為 DB staging purge worker。
8. 商品主檔的「停用」是 `isActive=false`，保留所有歷史且可重新啟用；「刪除」是受保護的 hard delete，只能由 `delete_uniform_item` 執行，任何 `inventory_balances`／`inventory_ledger_entries` 紀錄或其他業務外鍵關聯都必須拒絕。庫存管理的修改／刪除語意仍是修改草稿、取消未過帳工作或建立更正，不能直接 UPDATE／DELETE `inventory_balances` 或 append-only ledger。
9. System Guide 的入口效能依賴兩層設計：前台只用 RLS self-read `user_roles` 與 `uniform:system-guide-access:<user-id>` sessionStorage key 加速可見性，受保護文件永遠由 API 重新授權。角色撤銷後允許入口短暫存在，但 API 必須拒絕並清除快取；不要把文件內容存進 localStorage/sessionStorage，也不要恢復「首頁 HEAD 後才顯示、獨立頁 GET 後才建立文件按鈕」的雙延遲架構。
10. 共用管理清單的欄位隱藏、密度與每頁筆數只有顯示語意，不得改變匯出篩選集合、資料查詢或 RLS。識別欄與功能欄保持固定；員工匯出仍針對完整篩選結果，不只匯出目前頁面或可見欄位。
11. SSO 的驗收目標是已驗證工作入口：保持 `/login` 的 server-side Session guard、`/app` 的 server-side `auth.getUser()` guard；一般 SSO callback 先以 HttpOnly pending state 導向同源確認視窗，只有使用者確認後才由 server-side hash exchange 建立 target session，不能把 ticket、magic-link token 放入瀏覽器 URL、前端狀態或 Log。`SsoLoginPrompt` 失敗只顯示 allowlisted `diagnosticStage`；pending state 缺失時使用 `sso_pending_state_missing`；取消只保留原有登入表單，account binding 仍顯示綁定中並走既有登入／resume。提示動畫必須支援 `prefers-reduced-motion`，且不使用固定等待；既有非 SSO magic-link 才使用 `/auth/callback` 的 implicit 專用 fallback，必須清除 fragment 且拒絕 PKCE／混合 flow。每次測試都使用 Portal 新產生的 ticket。

### 已知交接界線

- 第一批正式帳號、角色指派、需求窗口範圍、GitHub/Vercel owner 與備援聯絡人仍待由使用者提供；管理模組本身已完成。
- 鼎新正式格式、正式 PDF 列印樣式與期初真實資料仍待外部資料；Renderer runner、Storage proxy/finalize 與 disposable-staging active smoke harness 已完成，但真實 Storage／RLS／signed URL／並行鎖 staging smoke 仍屬正式驗收事項。
- 商品／庫存模組的本機 UI、既有 RPC 串接、期初分流與庫存匯出已完成；`0076_inventory_report_export_audit.sql`（手動 SQL 副本在 `supabase/manual/`）與 `0085_uniform_item_delete.sql`（手動 SQL 副本在 `supabase/manual/`）仍須由使用者在已具備 `audit_events`、`operation_commands` 與 `private.append_audit_event` 的 Supabase project 執行，並以 HR／WAREHOUSE session 驗證匯出稽核與各操作 RLS。商品刪除請另以有庫存紀錄與無庫存紀錄的測試品號驗證拒絕／成功兩條路徑。
- Storage cleanup 的 DB contract、dry-run/execute runner、contract tests 與每日 dry-run workflow 已落地；`0072_import_terminal_retention.sql` 已提供 FAILED／CANCELLED source object 的 90-day DB eligibility，且不刪除 import DB evidence。`0074_import_staging_payload_retention.sql` 的 `job_import_retention` 90-day DB staging payload scrub contract，以及獨立 dry-run/execute runner、preflight 與每日 dry-run workflow 也已落地；runner 只呼叫 0074 薄 RPC，保留 row shell/chunk/diff 並留下 `staging_purged_at`。Storage／DB staging 兩種 destructive 模式仍必須在受保護 staging 以各自 job role 與雙 gate 實際驗收，尚未啟用 destructive cron。
- System Guide 已在正式站以 SYSTEM_ADMIN session 驗證三份文件、原左側導航、文件頁籤切換與返回總覽；工作區 shell 會預取 route，文件請求與角色自讀並行。既有量測為工作區導航後約 37ms 顯示入口、說明外殼後約 32ms 顯示文件按鈕；這些是 repository／UI smoke 觀察，不是 production performance evidence。未登入 API 會回 401；尚未以一個真實的「已登入但非 SYSTEM_ADMIN」正式帳號做瀏覽器 smoke，該情境目前由 server authorization 與 contract tests 保護。
- SSO server-side verified-entry 修正已推送並由既有 GitHub→Vercel 流程部署；正式站公開 health、匿名 `/app`／`/login` 與 callback 基本 smoke 已通過。仍須以全新、120 秒且一次性的 Portal ticket 驗收 `/api/sso-login` 登入／綁定後瀏覽器落在 `/app` 或角色導向工作入口，而不是公開 `/login`；若 fresh binding ticket 搭配有效目標 Session 回 `503 SSO_CONFIGURATION_ERROR`，依附件要求只做不含秘密的階段診斷，不重播 ticket 或加入 Google OAuth。若 Health 與 callback 都正常但中央控制台仍顯示 `testing`，只回報中央管理員將 `un` 改為 `active`，不要繼續改目標程式。
- 版面基線 commit 為 `3059723`；後續只調整視覺時保留 AP／MX／GS／MB／SH 五套選項與 `uniform:appearance-theme` localStorage key。

## 先讀什麼

1. 先讀 [`README.md`](./README.md) 的「正式應用接手狀態」、「Prototype 交接狀態」與「尚待提供」。
2. 再讀 [`CONTEXT.md`](./CONTEXT.md)，掌握業務詞彙與角色名稱。
3. 要修改正式功能時，依工作範圍讀 `docs/spec/`、`docs/architecture/`、`docs/implementation/roadmap.md`；要修改 System Guide 時先讀 [`docs/system-guide/README.md`](./docs/system-guide/README.md) 與三份同源文件。不要把 prototype 的記憶體資料當成正式資料來源。

## 目前工作邊界

- 正式 Next.js 應用可由 [uniform-co.vercel.app](https://uniform-co.vercel.app/) 瀏覽，但需要 Supabase env 與有效登入 session；正式資料與權限以 `src/`、`src/domain/`、`src/server/`、`supabase/migrations/` 為準。
- 可直接雙擊開啟的內容原型是 [`prototype/uniform-management-prototype.html`](./prototype/uniform-management-prototype.html)，為單一 HTML 檔，沒有 bundler、API 或資料庫依賴。
- prototype 的目的，是讓使用者確認版面、欄位、流程與命名；正式邏輯必須回到 `src/` 與 `supabase/migrations/` 實作。
- prototype 內容可以用 `apply_patch` 直接修改；保持單檔可雙擊開啟，避免引入需要安裝才能執行的依賴。
- 若需求只提到 prototype，修改範圍先限於 `prototype/uniform-management-prototype.html`、README.md 與本接手文件；不要擅自改 production migration 或資料庫契約。

## Prototype 不可破壞的資料契約

### 倉庫

只能使用兩個值：

- `人資倉`
- `總倉`

更新庫存、地圖、發放批次或入庫表單時，禁止重新引入台北／北區／中區／南區分倉等示例名稱。

### 機構

機構清單是現有資料，不要自行新增示例機構或重新排序。完整清單以 README 與 HTML 中的 `institutions` 常數為準：

`8C清福`、`7C清氣`、`6C清心`、`5C清平`、`3C清安`、`8D清春`、`7D清日`、`6D清照`、`5D清風`、`3D清景`、`8E青山`、`7E清泉`、`6E清水`、`5E清清`、`3E清涼`、`2CD清護`、`清福法人`、`一館`、`三館`、`二館`、`清田法人`、`清福幼兒園`、`含笑`、`其他`

### 人資需求

目前需求資料只使用：

- `no`
- `employee`
- `employeeNo`
- `institution`
- `items`
- `status`
- `tone`
- `updated`
- `note`

目前不使用 `department` 或人員 `owner`。需求清單、建立表單、詳情視窗都只顯示機構；後續若恢復部門，必須一次同步資料、表單、表格、詳情、篩選與測試。

需求狀態流程：

```text
送出申請 → 待主管核准 → 待 HR 備貨 → 預留完成 → 已完成
                         ↘ 退回補件 → 補齊資料後重新送出
```

`advanceRecord()` 會推進人資需求，`rejectRecord()` 會退回待補件；`syncRequestTask()` 會同步同一需求在總覽工作佇列的狀態。新增欄位或狀態時，這三個位置要一起檢查。

## 修改方法

1. 先搜尋既有常數與 render function，再做最小 patch；不要用整檔重寫覆蓋別人的修改。
2. 任何可見欄位變更都要同步資料模型、表格、詳情、表單與新增流程。
3. 對使用者提供的機構／倉庫名稱照字面保留，避免自動改成同義詞或示例名稱。
4. Prototype 不做持久化；若要測試狀態，使用同一個分頁完成流程，不要新增 localStorage 或後端依賴。
5. 文件要說明「目前已完成」與「仍需正式環境驗收」的界線，避免後續 agent 把 prototype smoke 當成 SQL/RLS/Storage 驗收。

## 交接驗證清單

修改後至少執行：

```cmd
node -e "const fs=require('fs'),vm=require('vm'); const h=fs.readFileSync('prototype/uniform-management-prototype.html','utf8'); new vm.Script(h.split('<script>')[1].split('</script>')[0]); console.log('prototype syntax: ok')"
npm run lint
npm test
npm run typecheck
npm run build
git diff --check
```

另外用瀏覽器或等價的 DOM smoke 確認：

- `?variant=A|B|C` 都能開啟。
- 六個 workspace 都能渲染。
- 人資需求詳情沒有負責人／部門欄位。
- 新建需求會保存員工編號與機構，並建立工作佇列項目。
- 需求詳情三段推進與退回補件都能更新清單與工作佇列。
- 倉庫只有 `人資倉`、`總倉`，機構清單為 24 個現有機構。

正式 Next.js App 另外確認：未登入時只呈現登入視窗；登入後七個 workspace 都能透過左側導航與網址 hash 切換；topbar 搜尋／通知可開啟；每個 workspace 的頁籤是內容切換；風格下拉選單包含 `AP`、`MX`、`GS`、`MB`、`SH`，切換後應寫入 `uniform:appearance-theme` 並在重新整理後保留；帳號管理需由 SYSTEM_ADMIN session 驗證後才可讀寫，且所有操作保留理由與 idempotency key。`/` 應導向 `/app`，匿名 `/app` 應由 server-side guard 導向 `/login`，已有 Session 開啟 `/login` 應直接進 `/app`，magic-link `/auth/callback` 建立 Session 後也應進 `/app`。SYSTEM_ADMIN 的「系統說明」入口應在首屏附近顯示，`/system-guide` 保留原工作區左側導航且三個文件按鈕不等待內容 API 才出現；未登入 `/api/system-guide` 必須回 401。

## 已知限制

- prototype 內資料是 mock snapshot，重新整理會重設。
- prototype 的「建立、核准、退回、匯出」只模擬 UI 狀態，不會呼叫 Supabase RPC。
- 正式 renderer、Storage、RLS、並行鎖序、匯入 worker 與鼎新格式仍以 migration、domain code、runbook 與 staging smoke 為準。
- 鼎新正式格式與正式列印樣式仍等待外部樣本，不能用 prototype 的示範 CSV 宣稱完成。

- 主檔快取 adapter 在 30 秒快照內可將完整商品／組織／員工讀取投影成啟用品號、機構、員工與採購選項；沒有完整快照時才走原本最小查詢，並只傳遞相關資源錯誤。`invalidateMasterDataCache` 由同一 seam 維護來源快照到投影快取的依賴失效（例如商品同步失效採購／啟用品號／人資選項），避免各 mutation 呼叫端漏清 sibling key。這個 seam 只改善讀取 latency，不改 RLS、mutation、冪等或資料庫權限。
- 2026-09-20 交易讀取與重試優化：`PurchaseReceiptPanel` 以 `lineSelectionLoading` 區分採購單清單背景刷新與目前明細查詢，保留可用明細時不鎖住明細選擇；`ReplenishmentPanel` 以帳號綁定的 `itemsReadBlocked`／`dataReady` 判斷品號選項是否真的可用，暖資料背景同步或暫時 JWT 不同步會保留既有草稿，首次載入、帳號切換與權限／Schema 失敗才 fail-closed。補庫取消以同一張草稿的固定 cancel key 重試，修改取消原因才輪換 key；契約測試必須覆蓋這兩個 seam。
- 2026-09-20 補庫送出往返優化：`ReplenishmentPanel` 改走 `src/domain/replenishment-submission.ts`，優先以單一 `submit_replenishment_request_with_lines` RPC 原子完成新建／更新與送出；`0130_atomic_replenishment_submission.sql` 僅委派既有草稿／送出函式，不改寫 HR 權限、庫存鎖、稽核或狀態規則。只有明確缺少 wrapper 時回退既有 RPC；結果未知時鎖住輸入並沿用相同冪等鍵重試。正式 Supabase 尚須依 migration workflow 套用 0130，Git push 不會更新 DB。
- 2026-09-20 人資需求讀取恢復優化：`HrRequestWorkbench` 以帳號綁定的員工／品號選項快照作為操作邊界；同一帳號已有完整快照時，背景重讀或暫時 JWT 不同步會保留員工明細、增庫量與草稿，送出不再被 `loadingData` 無條件鎖住。初次尚未取得快照、帳號切換或權限／Schema／資料錯誤才隱藏失效選項並停止送出；正式送出仍走既有 RPC，由伺服器重新驗證庫存與員工有效性。契約由 `hr-request-ui-contract.test.ts` 保護。
- 2026-09-20 員工目錄已有成功快照時，`EmployeeCatalogPanel` 的背景刷新不再阻止目前篩選結果匯出；只有初次尚無資料時才停用匯出，重新整理按鈕仍避免重複讀取。這維持匯出稽核與完整篩選集合不變，只縮小讀取狀態的影響範圍。
- 2026-09-20 新增 `0123_correction_history_line_indexes.sql`：更正歷史 unified view 使用的退回、人資發放、兩種調撥與盤點來源明細各有反向來源索引；入庫來源沿用 0100。這是純加法 read-path migration，必須手動套用並依既有 migration／RLS workflow 驗證，不能把 Git push 視為資料庫已更新。
- 2026-09-20 換季審核／採購決策讀取恢復優化：已有清單快照時，背景刷新不再鎖住「選取」或顯示空白骨架；CEO 審核保留仍存在的明細，採購決策保留完整供應商／MOQ 快照。暫時 JWT 同步錯誤才保留快照，權限／Schema 錯誤會清除不可再信任的資料；契約由 `seasonal-read-path-contract.test.ts` 保護。
- 2026-09-20 CEO 審核快照帳號邊界：`SeasonalApprovalPanel` 將 PENDING 待核版本與選取後明細綁定目前 `accountId`；同一帳號的暫時 JWT 同步錯誤可保留待核清單，切換帳號、首次尚未完成讀取或權限／Schema 失敗時隱藏舊版本並停止核准／退回，revision、snapshot hash 與 `review_seasonal_submission` RPC 不變；`seasonal-read-path-contract.test.ts` 保護此 seam。
- 2026-09-20 帳號管理目錄快照帳號邊界：`AccountAdminPanel` 將帳號／角色目錄快照綁定目前工作區 `accountId`；切換管理者、初次尚未完成讀取或目錄讀取失敗時隱藏舊清單，避免跨帳號顯示上一個管理者的資料。帳號 API、Auth Admin、角色／窗口範圍與稽核契約不變；`account-admin-workbench-contract.test.ts` 保護此 seam。
- 2026-09-20 人資需求查詢首屏與帳號邊界：`HrRequestHistoryPanel` 將需求單清單快照綁定目前 `accountId`；同一帳號的暫時 JWT 同步錯誤保留清單，切換帳號、首次尚未完成讀取或權限／Schema 失敗時隱藏舊資料並停止明細查詢。首次清單讀取不再自動選第一筆，使用者選取需求單後才查詢 `v_hr_request_history_detail`，避免不必要的首屏明細讀取；`hr-request-read-path-contract.test.ts` 保護此 seam。
- 2026-09-20 採購編輯 draft 保留：`SeasonalProcurementPanel` 背景刷新完成時，若目前選取品項、供應商仍有效且採購決策版本未變，僅更新唯讀清單／快照，不覆蓋使用者正在輸入的採購量、差異理由、備註與採購單欄位；品項消失、供應商失效或外部決策版本改變才重新校正工作區。伺服器 RPC 仍重新驗證 MOQ、核准量與版本。
- 2026-09-20 總覽帳號快照與局部刷新：`OverviewDashboard` 將核心／延後摘要綁定目前 `accountId`；身份切換或重新就緒會強制完整讀取並在切換期間隱藏舊帳號資料，同一帳號的暫時 JWT 同步錯誤才保留既有摘要。事件仍依 `overviewReadPlanForScope` 只讀受影響來源，不得恢復六組 reporting view 全量重查；`reporting-read-path-contract.test.ts` 保護帳號快照、retry 與讀取 scope。
- 2026-09-20 採購原因碼讀取邊界：`ProcurementReasonCodePanel` 將原因碼清單綁定目前 `accountId`，切換帳號或不可恢復的讀取錯誤時隱藏舊清單，同一帳號的暫時 JWT 錯誤才保留快照；重新整理按鈕與背景讀取不再把已有清單變成空表 loading，保存仍只走 `maintain_procurement_difference_reason`。`procurement-reason-read-path-contract.test.ts` 保護此 seam。
- 2026-09-20 清單讀取競速共用 seam：商品、組織、員工、兩倉庫存、營運報表與採購原因碼改用 `createReadRequestController` 管理讀取 token；快速切換帳號／報表或重新整理時，舊回應不會發布 rows，也不會提前關閉新請求的 loading。這個純讀取 seam 不改 RLS、匯出稽核、mutation RPC 或資料庫狀態機；`read-refresh.test.ts` 與 `read-refresh-ui-contract.test.ts` 保護其介面。
- 2026-09-20 durable import queue 讀取加速：`0124_durable_import_work_queue_index.sql` 依 `list_import_work()` 的 active statuses 與 `created_at,id` 排序建立部分索引，排除 terminal history 對 worker queue scan 的影響。此 migration 不改 lease、chunk、APPLY、RLS 或 retention；正式 project 必須另依 migration workflow 套用，契約由 `durable-import-read-path-contract.test.ts` 保護。
- 2026-09-20 採購快照帳號邊界：`SeasonalProcurementPanel` 將核准品項、供應商／MOQ、差異原因碼與既有決策快照綁定目前 `accountId`；同一帳號的背景刷新可保留 draft，切換帳號、首次尚未取得快照或權限／Schema 讀取失敗時先隱藏舊資料並停止保存，避免沿用上一個帳號的選項或冪等狀態。契約由 `seasonal-read-path-contract.test.ts` 保護。
- 2026-09-20 換季重新載入解除卡控：四個換季 panel 的重新載入按鈕不再被資料讀取狀態禁用，只在 mutation `busy` 時停用；採購 queue／master-data 與 CEO 審核 queue／明細以獨立讀取 controller 丟棄晚到回應，快速重整或切換審核版本不會覆蓋新 rows、編輯器或 loading。活動／需求仍沿用既有序號與快照防線，所有 RPC、RLS 與交易狀態機不變；`seasonal-read-path-contract.test.ts` 保護此 seam。
- 2026-09-20 人資需求歷史重試解除卡控：`HrRequestHistoryPanel` 的唯讀「重新整理」不再因 `loading` 停用，慢查詢時可立即再次發起讀取；既有 `historyReadSequenceRef`／`detailReadSequenceRef` 讓最新查詢擁有 loading，晚到回應只會被丟棄，不覆蓋清單／明細或提前關閉新查詢。需求查詢 view、RLS、預留、mutation 與稽核契約不變；`hr-request-read-path-contract.test.ts` 保護此 seam。
- 2026-09-20 全站唯讀重整解除卡控：商品、組織、員工、兩倉庫存、營運報表、採購原因碼與人資需求的重新整理不再被 `dataLoading`／`loadingData` 停用，只在 mutation `busy` 時停用；既有 catalog/report controller 或 HR effect cleanup 負責丟棄晚到回應。帳號目錄新增 `directoryLoadSequence`，重複重整時只有最新讀取能發布帳號／角色資料與關閉 loading。主檔／報表 view、RLS、需求選項與交易 RPC 不變；`read-refresh-ui-contract.test.ts`、`hr-request-ui-contract.test.ts` 與 `account-admin-workbench-contract.test.ts` 保護此 seam。
- 2026-09-20 採購入庫快照帳號邊界：`PurchaseReceiptPanel` 將採購單來源清單綁定目前 `accountId`；同一帳號的暫時 JWT 同步錯誤可保留上次成功清單，切換帳號、首次未完成讀取或權限／Schema 失敗時隱藏舊來源與入庫草稿並停止 RPC 操作。採購單與明細仍分開 loading，草稿／POST／冪等鍵契約不變；`receipt-read-path-contract.test.ts` 與 `warehouse-operation-contract.test.ts` 保護此 seam。
- 2026-09-20 採購入庫單次往返：完整資料的一鍵完成使用 `completePurchaseReceipt`，優先呼叫新增 `complete_purchase_receipt` transaction RPC，把既有建立草稿與 POST RPC 收斂成一次瀏覽器往返；缺少新函式時才 fallback，且只有舊流程 POST 失敗才顯示可恢復的 DRAFT。`0126_atomic_purchase_receipt_completion.sql` 需另外套用到目標 Supabase project，否則仍使用相容的兩次呼叫；未完整分類的資料仍保留手動草稿路徑。
- 2026-09-20 制服退回快照帳號邊界：`ReturnPanel` 將已發貨需求、發放明細與退回原因碼綁定目前 `accountId`；同一帳號的暫時 JWT 同步錯誤保留既有快照，背景更新不鎖住已有原因碼的選擇，切換帳號、首次未完成讀取或權限／Schema 失敗時隱藏舊來源／選項並停止建立。新建退回草稿前以目前帳號快照確認原因碼，資料庫 RPC 仍重驗原因碼為啟用狀態；`read-refresh.test.ts` 與 `warehouse-operation-contract.test.ts` 保護此 seam。
- 2026-09-20 換季採購讀取競速 fencing：`SeasonalProcurementPanel` 的 queue／master-data 讀取改用 `createReadRequestController`；手動重新載入會先 invalidate 舊 token，晚到回應不會覆蓋最新 rows／編輯器或提前關閉新讀取的 loading，既有選取按鈕仍只受 mutation `busy` 控制。採購 queue view、master-data cache、RPC、RLS 與資料庫交易契約不變；`seasonal-read-path-contract.test.ts` 保護此 seam。
- 2026-09-20 庫存可用量快照帳號邊界：`InventoryAvailabilityPanel` 將 `v_item_availability` 成功讀取綁定目前 `accountId`；切換帳號或不可恢復的權限／Schema 錯誤時隱藏舊列，暫時 JWT 同步錯誤仍保留清單。篩選、排序、分頁、匯出與庫存交易契約不變，`read-refresh-ui-contract.test.ts` 保護此 seam。
- 2026-09-20 報表快照帳號邊界：`ReportingPanel` 將上一份 report rows 綁定目前 `accountId`；同一帳號切換報表仍保留上一份欄位一致快照，切換帳號或不可恢復讀取錯誤時先隱藏舊列。報表 catalog allowlist、security-invoker views 與唯讀權限不變；`reporting-read-path-contract.test.ts` 保護此 seam。
- 2026-09-20 主檔清單快照帳號邊界：`ProductCatalogPanel`、`OrganizationCatalogPanel` 與 `EmployeeCatalogPanel` 將成功清單綁定目前 `accountId`；同一帳號的暫時 JWT 同步錯誤可保留快照，切換帳號、首次未完成讀取或權限／Schema 失敗時隱藏舊列並停止相依操作，員工匯出只允許使用目前帳號完整快照。主檔 RPC、RLS、唯一鍵與匯出稽核不變；`read-refresh-ui-contract.test.ts` 保護此 seam。
- 2026-09-20 商品／組織主檔編輯器讀取恢復優化：`ProductMasterEditorPanel` 與 `OrganizationMasterEditorPanel` 以完整選項快照決定可操作範圍；背景刷新不再鎖住既有商品資料或機構選擇，非暫時性錯誤會清除快照，部門保存仍必須先取得機構 UUID 對照。契約由 `master-data-editor-ui-contract.test.ts` 與 `master-data-cache-contract.test.ts` 保護。
- 2026-09-20 商品供應關係保存鎖定修正：`ProductMasterEditorPanel` 的 `SUPPLIER_ITEMS` 保存按鈕只在尚未取得完整供應商／品號快照時停用；已有快照的背景刷新不再把 `dataLoading` 誤當成不可保存，RPC、RLS、唯一鍵與伺服器端主檔驗證不變。
- 2026-09-20 五個更正工作台讀取恢復優化：`HrIssueCorrectionPanel`、`ReturnCorrectionPanel`、`PurchaseReceiptCorrectionPanel`、`WarehouseTransferCorrectionPanel` 與 `StocktakeCorrectionPanel` 以 `sourceSnapshotReady` 區分首次來源讀取與背景刷新；已有來源快照時，暫時 JWT 同步錯誤保留可用明細，來源選擇、單號、差額與理由不會被低頻刷新整段鎖住。非暫時性權限／Schema 錯誤會清空來源並 fail-closed，建立／POST、冪等鍵、歷史與交易 RPC 不變；契約由 `read-refresh-ui-contract.test.ts` 保護。
- 2026-09-20 員工編輯器讀取鎖優化：既有員工已有機構代碼時，背景讀取機構／部門選項不再停用歸屬選單或保存入口；只有新增模式尚未填機構時仍受控等待必要選項。最終關聯與角色權限仍由既有 server-side／RPC 驗證。
- 2026-09-20 盤點讀取鎖優化：`StocktakePanel` 以主檔快照與目前倉別帳面快照分開判斷依賴；同倉別已有快照時，背景 JWT 同步／餘額重讀不再鎖住單號、備註、既有明細或既有草稿，首次載入、切換新倉別與權限／Schema 失敗仍停止新增／送出並安全提示。盤點建立／更新／過帳仍由既有 RPC 重新鎖定品號與 balance version，未放寬任何資料一致性防線；契約由 `warehouse-operation-contract.test.ts` 保護。
- 2026-09-20 營運總覽重整解除卡控：`OverviewDashboard` 的重新整理只受 `identityReady` 控制，不再因核心／延後摘要 `displayLoading` 停用；既有 `queueReload` scope 合併與 effect cleanup 保護重複查詢，新增 `aria-busy` 供讀取狀態呈現。`v_overview_core`、reporting views、RLS、事件刷新與摘要語意不變；`reporting-read-path-contract.test.ts` 保護此 seam。
- 2026-09-20 工作流狀態讀取去重：`useWorkflowStatusPoll` 現在讓 PDF／ERP 的背景輪詢與手動重新查詢共享 in-flight promise；同一 artifact 不再因前景按鈕與背景輪詢同時發出兩個 status RPC，終態、錯誤重試、下載與產檔契約不變；`workflow-polling-contract.test.ts` 保護此 seam。
- 2026-09-20 帳號設定範圍讀取解耦：`AccountAdminPanel` 將 `coordinator_scopes` 與機構／部門選項的 loading 分離；已有選項快照時，切換帳號或重讀選項不鎖住選擇／窗口操作，窗口保存只等待目前帳號 scope 讀取完成，不等待無關的選項刷新。切換帳號仍會清除舊 scope 與選擇，序號仍丟棄舊回應，server-side 權限與 RPC 驗證不變；`account-admin-workbench-contract.test.ts` 保護此 seam。
- 2026-09-20 Durable import 重新查詢解耦：`DurableImportPanel` 的唯讀「重新查詢批次」不再因 `dataLoading` 停用；最新 `ReadRequestController` 與 `reloadSequenceRef` 共同丟棄晚到查詢及舊提示，使用者可在卡住時立即重試。完整逐列差異仍是「確認後匯入」的必要 gate，固定 Storage key、冪等鍵、Edge worker 與 APPLY 狀態機不變；契約由 `durable-import-ui.test.ts` 保護。
- 2026-09-20 入庫／退回明細 loading 修正：`PurchaseReceiptPanel` 與 `ReturnPanel` 在清空來源、切換帳號、空清單或不可恢復讀取錯誤時同步清除舊明細並釋放 `linesLoading`，避免被已取消且沒有下一個 effect 的相依查詢永久卡住；選取新來源仍由最新 effect 載入，交易來源、POST、RLS 與冪等契約不變；`warehouse-operation-contract.test.ts` 保護此 seam。
- 2026-09-20 盤點餘額 loading 範圍修正：`StocktakePanel` 不再使用跨倉別共用的 loading boolean，而以目前選取倉別與有效 panel／資料快照範圍判斷讀取中；清空／切換倉別、切換帳號或離開 panel 時，舊查詢 cleanup 不會讓畫面停在轉圈狀態。若帳面餘額快照尚未匹配目前倉別，新增／送出仍 fail-closed；盤點 RPC、版本 fencing、RLS 與冪等不變，`read-refresh.test.ts` 與 `warehouse-operation-contract.test.ts` 保護此行為。
- 2026-09-20 退回來源切換解除背景讀取鎖：`ReturnPanel` 在目前帳號快照仍有效時，背景重新整理不再停用已發貨需求選單；切換來源仍會清除舊發放明細，且 `requestsLoading`／`linesLoading` 繼續阻擋建立／完成，首次讀取與帳號／權限失效仍 fail-closed。契約由 `warehouse-operation-contract.test.ts` 保護。
- 2026-09-20 盤點一鍵完成單次往返：`StocktakePanel` 以 `completeStocktakeOperation` 優先呼叫 `complete_stocktake`，在單一 transaction 委派既有草稿建立／更新與 POST；既有保護與 `STALE_COUNT` 重盤流程不變。目標 Supabase 尚未套用 `0127_atomic_stocktake_completion.sql` 時，僅明確缺少函式才回退並快取該能力結果；未知回應鎖定表單並保留冪等鍵供同資料重試。adapter／migration 契約由 `stocktake-completion.test.ts` 與 `warehouse-operation-contract.test.ts` 保護。Migration 需透過既有 Supabase migration workflow 另行套用。
- 2026-09-20 人資需求送出往返最佳化：新單／DRAFT 先寫後送改由 `submitHrRequestOperation` 優先走單次 `submit_hr_request_with_lines`；詳細鎖序、fallback 與回應遺失契約見 `docs/architecture/module-workbench.md` 第 37 點。0128 migration 尚待 Supabase 套用。

最後更新：2026-09-21。倉庫發貨清單刷新不得把尚未載入明細的第一列標成目前作業；若目前選取來源不再存在，必須同步清空右側編輯器與冪等狀態，避免顯示或操作過期明細。讀取明細不得沿用 mutation `busy` 鎖住整個模組，必須以 `detailsLoading`／等價狀態只鎖定右側明細編輯與主要操作；左側來源選擇可在純明細讀取期間繼續操作，`detailsReadController` 必須丟棄晚到回應。採購入庫的採購單與明細查詢也要維持獨立 loading。兩倉庫存清單使用 `dataLoading`，員工清單分開目錄讀取與匯出 busy，保留快照時不得把查詢狀態誤標成寫入處理中。補庫正式模式不得以 DEMO 品號作初始資料；以帳號／品號快照區分首次未載入、帳號切換／非暫時錯誤與暫時 Session 同步，後者保留草稿且不鎖住可用操作，只有預覽模式可以顯示 DEMO。營運報表是純唯讀查詢，使用 `dataLoading` 而非 mutation `busy`；切換報表不得鎖住搜尋，重新整理與空表骨架只在必要讀取期間受控，上一份成功快照要保留到新報表完成或確認失敗。PDF／ERP 的單據／機構選項與 artifact 狀態查詢使用獨立 `documentLoading`／`institutionLoading` 與 `artifactLoading`；PDF 成品類型不因單據清單讀取而被鎖住，前景重新查詢與背景 polling 共用 in-flight promise，讀取中可再次查詢而不增加 RPC；READY 成品下載不受 artifactLoading 阻擋，下載 RPC 仍重驗角色／狀態；背景 polling 不鎖住表單，建立／下載仍使用 mutation `busy`。帳號清單重新整理只使用 `directoryLoading`／`manageDataLoading`，不得用 mutation `busy` 鎖住新增與帳號設定表單。商品／組織清單的唯讀刷新使用獨立 `dataLoading`，不得把主檔查詢當作 mutation `busy`；已有快照時保留搜尋與篩選，重新整理要防止重複請求並提供 `aria-busy`／`aria-live` 狀態。員工／部門若已有既有歸屬代碼，保存／停用不因選項清單讀取而整體鎖住；新增且尚未有必要機構代碼時仍維持受控。五個更正工作台的來源讀取使用獨立 `dataLoading`／`aria-live`；來源未完成時只停用來源選擇與提交，單號、差額與原因欄位仍可填寫，不得將低頻來源查詢當成 mutation `busy`。若下一次需求改變倉庫、機構、需求欄位、流程、管理清單、主檔角色契約或 System Guide 的權限／載入架構，先更新本文件與 README 的契約，再修改 prototype 或正式功能，最後重新跑驗證清單。
